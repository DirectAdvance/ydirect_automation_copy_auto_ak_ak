# Слепки — изменения 2026-07-14 + основа плана пересбора/дополнения

> Рабочий документ. Что сегодня ТРОГАЛИ по слепкам (состояние изменилось), что СОБРАНО в staging
> (готово дополнять, ещё НЕ в паке), и диагнозы/правила, которые план обязан учесть.
> Инфра на момент фиксации: LXC101 ✅ Tailscale, M3 троттлит новые ssh, гео-чистка идёт.

---

## A. ФАКТИЧЕСКИЕ ИЗМЕНЕНИЯ пака (состояние слепков стало иным, чем утром)

| # | Слепок / область | Что изменено | Где | Статус |
|---|---|---|---|---|
| A1 | **ВСЕ слепки** — гео-чистка ключей | Удалён город/область из ключей + дедуп коллизий внутри группы (чтобы «Самара»-ключ не улетал на Кемерово) | DST + RAW + **M3** (md5 совпали) | ✅ ЗАВЕРШЕНА 01:42 (10925 файлов, refresh_index OK). ⚠️ ОСТАТОК: города в МИНУС-частях фраз не вычищены (`...-Екатеринбург -Кемерово`) — до-чистка минусов при необходимости |
| A2 | **kuderko / С пробегом / tp4** | Ключи залиты в `ct0006` (3762, кредит/рассрочка) и `ct0013` (1146, салон) по интент-регексам | M3 + DST + RAW | ⚠️ **СХЛОПНУТО — ОШИБКА.** Под откат/переделку 1в1 (см. C1). 170 реальных групп слиты в 2 ct |

**Вывод для плана:** A1 — сохранить (полезно всем). A2 — переделать: заменить схлопнутые ct0006/ct0013 на реальные под-группы 1в1 из staging.

## B. UI (не данные — для полноты) — ✅ ЗАДЕПЛОЕНО
- detail-панель `_slRenderDetailCard`/`_slDetailKwView`: tp2/tp4 бейдж→**КС**, текст «КС-кампания — ключевые слова ещё не собраны в паке» вместо ложного «Автотаргетинг».
- ⚠️ **Файл сменился 2026-07-17:** UI вынесен из `templates/direct/index.html` в `static/direct/slepki_ui.js`. Актуальные адреса — `slepki_ui.js:1224` (`_slRenderDetailCard`) и `:1133` (`_slDetailKwView`), текст КС — `:1153`. Старые номера строк по index.html недействительны.
- Статус деплоя проверен 2026-07-19: md5 `slepki_ui.js` на Mac == LXC 101, `direct-slepki.service` и `direct-slepki-worker.service` — `active`.

---

## C. СОБРАНО В STAGING (на маке, целостно, ещё НЕ в паке — сырьё для дополнения)

Корень: `scratchpad/kuderko_harvest/` и `scratchpad/{dmp,genses}_restore/`.

| # | Что | Объём | Путь |
|---|---|---|---|
| C1 | **kuderko tp1-tp4 per-group 1в1** | ~14k+ групп, ~78k ключей, 14 логинов | `kuderko_harvest/by_login/{g1,g2,g3,g_raw}/<login>/<camp>/<ag>/keywords.txt+meta.json` |
| C2 | **kuderko tp6 (МК)** | 222 камп / 273 гр / 179 UAC-dirs, 80 заголовков, 1902 МК-ключей, 690 картинок-URL | `kuderko_harvest/staging/kuderko/tp6/` |
| C3 | **kuderko tp7 (Товарка)** | 140 камп / 141 гр / 119 UAC-dirs | `staging/kuderko/tp7/` |
| C4 | **kuderko tp2 (Поиск КС)** | 108 групп ключей (4 логина) | `staging/kuderko/tp2/<login>/` |
| C5 | **kuderko callouts (уточнения)** | 14 | `staging/kuderko/callouts/kuderko.txt` |
| C6 | **dmp POISK-выгрузки** (восстановление) | 5 xlsx | `scratchpad/dmp_restore/*.xlsx` |
| C7 | **gen_ses выгрузки** | 6 xlsx | `scratchpad/genses_restore/*.xlsx` |
| C8 | **новый кодер «ct need-number»** | ct0800…ct0834 + ct0032/ct0084 | `scratchpad/genses_coder.csv` |

**Недоступно (для плана — что не добрать без действий):** `porg-edmpebhr` (error 53 — чужое агентство, нужен OAuth); tp6/tp7 creative 4 логинов (нет managing-cookie); фото/видео kuderko (URL в manifest, не скачаны).

---

## D. ДИАГНОЗЫ и ПРАВИЛА, которые план обязан соблюсти

- **D1. Группы 1в1 по СОСТАВУ.** Не схлопывать. Разный состав (ключи/таргетинг) → отдельная группа даже при ОДИНАКОВОМ gc (коллизия gc = НОРМА). Идентичный состав, разница лишь в стратегии оплаты (клики/конверсии) → ОДНА ветка. Группа не подходит под кодер → писать БЕЗ gc, просто по имени.
- **D2. Движок не поддерживает 1в1 сейчас** — пак ключён по `ct`, gc не различает под-группы (ag=возраст, g=пол). Нужна доработка: group-key отдельно от gc + per-adgroup раскладка + gather/циклы group-aware + фолбэк. (Фаза 1, `kontent_pack.py`/`create_set_text_builders.py`/`create_set_tp1_builders.py`/`slepki_editor.py` + per-slepok файлы `direct/slepki/<key>.json` через `slepki_store`).
  ⚠️ Монолита `slepki_structure.json` больше нет (разбит 2026-07-17) — см. пометку superseded ниже по файлу.
- **D3. dmp — рассинхрон кодера.** Структура на КАНОНе ct0800–ct0834, пак/M3 на ct0001–ct0034. Фикс: переименовать НА M3 `dmp/tp2/ct00NN → ct0(NN+799)` (+dmp.txt, dmp_minus_shared.txt; ct0000 не трогать). gen_ses — цел.
- **D4. Prune «из раза в раз».** `sync_content_m3.py` (3×/сутки, rsync --delete + prune) стирает всё, что писали мимо M3. Любая заливка в пак → в **M3** (или ждать two-way синк, решён Семёном: 101↔M3 two-way, медиа M3→101 сжатые).
- **D5. Реальная структура ≠ текущая.** Пример kuderko/tp4: реально интент-кампании (Автокредит 46 / Автокредит_бу 67 / Рассрочка 36 / Автосалон 21 под-групп × регионы), в структуре было 5. tp6/tp7 у kuderko ЕСТЬ (видны только по кукам/Grid, не v5 API).

---

## E. СОГЛАСОВАННЫЙ ПЛАН СБОРА (2026-07-14, с Семёном) — ✅ СБОР ЗАВЕРШЁН 2026-07-14
**Цель:** пересобрать/обновить эталонную структуру слепков из живых кабинетов, per-group 1в1.

### ✅ РЕЗУЛЬТАТ СБОРА (2026-07-14) — 12/12 слепков, залито на 101, верифицировано
- **tp1-5:** 119 313 групп per-group 1в1 по всем 12 слепкам (Щербакова/Павлов/Терехов/Тумашенко/Крючкова/
  Саламахин/Караваев/Кудерко/Зубакин/Гордеева/Чепелев/Питеркина). Топ-5 гео-разнообразных на пару (слепок,тип).
- **tp6/tp7:** задетектированы grid-only во ВСЕХ 12 (v5 их НЕ показывает — доказано; детект = id в `grid_list_campaigns`,
  нет в v5). Имена→tp/gc/таргетинг в `<slug>/_tp67_uac.json`. **tp8 Telegram — исключён** (решение Семёна: только tp1-7).
- **Перенос:** `/opt/slepki_harvest_2026-07-14/` на LXC101 — Mac==101=358697 файлов, md5 ✅, **ВНЕ cron-зоны**
  `sync_content_m3.py` (не сотрёт). Staging (не M3/пак) — запись в M3 будет на фазе загрузки F.4.
- **Тулинг сбора:** `scratchpad/slepki_harvest_2026-07-14/_harvest_v5.py <slug>` (tp1-5, resume, site/login-фильтры),
  `_grid_tp67.py <slug>` (tp6/7 grid-only). Кука ТОЛЬКО через `campaign.pick_working_cookie` (живая, главпоток), НЕ `cookies.json`.
- **Попутные фиксы:** `geo_strip.py` — убран висячий предлог после удаления города (self-test OK, НЕ задеплоен);
  callouts баг `adextensions.get` (нужен `CalloutFieldNames:["CalloutText"]`).
- **Открытое для загрузки:** old-naming (`nord_/ugob_/auno_/avma_/ceav_`)→tp-правило; дедуп дублей `_копия`; gc (G5); дедуп tp6/7-версий.
- Детали/трекер: `scratchpad/slepki_harvest_2026-07-14/_PROGRESS.md`.

### E.1 Источник и охват
- Аккаунты из **`public.local_gsheet_sites`** (Victory). Слепок = колонка **`directologist`**.
- **12 авто-слепков:** Щербакова, Павлов, Терехов, Тумашенко, Крючкова, Саламахин, Караваев, Кудерко, Зубакин, Гордеева, Чепелев, Питеркина.
- **`site_type`: 4 рабочих** — Мультибренд, Монобренд, С пробегом, Мульти+БУ. ⛔ Исключить **Квиз, Неопределено, «Не трогать!»** (правило CLAUDE.md).
- **Статус НЕ учитываем** (берём логины любого статуса, вкл. Удалён/Стоп — там есть эталонная структура).
- **ГенСес/ДМП — пропустить** (лидоген, нет в `directologist`; своя механика ct08xx — отдельной задачей позже).

### E.2 Отбор аккаунтов
- **Топ-5 аккаунтов на пару (слепок, site_type)** — по кол-ву кампаний, но взятых из **РАЗНЫХ гео** (покрыть гео-вариативность ключей без взрыва объёма).
- ⚠️ **Счётчик кампаний — через Grid (куки), НЕ `campaigns.get` v5**: v5 не показывает tp6/tp7 (МК/Товарка/UAC) → топ-5 исказится. Где кук нет — v5 + пометка неполноты.

### E.3 Параллелизм — по КАНАЛУ доступа (не по агентству: почти всё = 1 агентство «Виктори»)
Факт: 1134 логина / 12 слепков — агентство **«Виктори»** (остальные единичны). «1 поток на агентство» токенами = 1 токен-поток на всё → узкое место v5. Решение — **параллелить по кукам И токенам** (разные лимиты):
- **Куки-канал (Grid) — ПАРАЛЛЕЛЬНО, N потоков** (основной параллелизм, по слепкам/типам/логинам). Куки per-account → лимит Виктори не бьют. Собирает: tp6/tp7, счётчик кампаний (для топ-5), tp1-5 группы/ключи где Grid отдаёт.
- **Токен-канал (v5 API) — 1 ПОТОК (Виктори), последовательно**, идёт параллельно куки-каналу. Добирает keywords tp1-5, что по кукам не вышло. Throttle + backoff + батч `adgroups.get`=10. Все v5-запросы сериализовать (1 victory-поток) — иначе rate limit.
- **Правило №3 (Семён):** не больше 1 потока на агентство токенами. Куки — параллелить свободно.
- Kuderko — **пересобрать единообразно** тем же пайплайном (не использовать старый staging C1-C5).

### E.4 Что собирает агент (per-group 1в1)
- Все tp: tp1-tp5 (v5 API, батч `adgroups.get`=**10**), tp6/tp7 (**Grid по кукам**).
- По КАЖДОЙ реальной ad-группе: имя группы + ключи + минусы (кампания+адгруппа) + таргетинг + тексты + уточнения/callouts + сайтлинки + картинки/видео (URL manifest).
- **Правила D1:** коллизия gc = норма; группа не под кодер → по имени без gc; НЕ схлопывать по интенту/ct.
- Формат staging единый: `scratchpad/slepki_harvest_2026-07-14/<slepok>/<site_type>/<login>/<camp>/<ag>/keywords.txt+minus.txt+meta.json`.

### E.5 Доступ к аккаунтам — ПЕРЕБОР токенов/кук
- Если основной токен/кука к аккаунту НЕ подошли — **перебрать ВСЕ** директ-токены из `.secret/.env` (y-direct-victory, victorylotsofads1, victoryagency14, victoryagencydirect, usefulcall, useful-call-agency, victoryagency-direct1618440) + все Grid-куки, пока не пробьёт (доказано на kuderko: доступ открывался разными токенами).
- ⚠️ `error 4001` на adgroups/keywords — это НЕ отказ доступа, а батч >10 CampaignId. Сначала батч=10, только потом вывод о недоступности.
- Реальные блокеры: `error 53` (аккаунт в чужом агентстве — нужен OAuth) / протухшие куки Grid. Такие — фиксировать в список, НЕ зацикливаться (1 полный перебор токенов+кук на логин достаточно).

### E.6 Хранение staging — СНАЧАЛА всё на мак, потом перенос на 101 (Семён)
- **Весь сбор пишем ЛОКАЛЬНО на мак:** `scratchpad/slepki_harvest_2026-07-14/<slepok>/<type>/<login>/<camp>/<ag>/`. Агенты НЕ пишут на 101 по ходу.
- **Перенос на LXC101 — ОДНИМ шагом ПОСЛЕ завершения сбора** (главная сессия): `rsync` мак → `/opt/slepki_harvest_2026-07-14/` (⚠️ ВНЕ cron-зоны: НЕ `/opt/neuro_content_local`, НЕ `/opt/neuro_content_raw`). Диск 101 после +50G = 59G свободно.
- Цель: пока сбор идёт — единая точка на маке (проще, не мусорим на 101); 101 получает готовый результат.

### E.7 Медиа — только сжатые (решено)
- Картинки/видео, ЕСЛИ есть, — скачиваем и храним **только сжатыми (80% качества)**: картинки JPEG q80, видео ffmpeg (libx264/yuv420p/aac/+faststart, ≤9.9МБ). Механика как `sync_content_m3.py` (`_compress_image` / `_normalize_pool_video`). Оригиналы не храним (экономия диска).

### E.8 Границы
- **Только СБОР на мак.** НЕ писать в M3-пак, НЕ править `slepki_structure.json`, НЕ деплоить (заливка — после движка 1в1 + через M3, отдельная фаза).
- **🛑 Сервисы `direct-create` / `direct-create-worker` / `digest` — НЕ трогать и НЕ перезапускать до последнего** (финальный деплой, когда всё готово). Сбор их не касается.
- Дубли определяем по СОСТАВУ таргетинга, не по неймингу: 2 кампании РСЯ с разницей только в картинках = ДУБЛЬ (одна структура, картинки объединяем/дедуп). Разный префикс + одинаковый таргетинг = дубль.
- Мердж при дублировании кампании: берём версию с БОЛЬШИМ числом групп (полнее).
- Медиа: скачиваем только из ОДНОГО аккаунта в рамках (слепок,тип), сжатые 80%, дубли картинок удаляем (по хешу).
- Гео-добавки (города) удаляем и из позитива, и из МИНУС-частей фраз (G8).
- `login_key`, встречающийся в >1 слепке — НЕ берём (неоднозначные, G9).
- gc собранным группам — пока как в текущих слепках (gsheet_naming), ошибки исправим потом (G5).

---

## G. ОТКРЫТЫЕ ВОПРОСЫ / РИСКИ (прогон плана на пробелы 2026-07-14)

### Статусы (все решены Семёном 2026-07-14)
| # | Риск | Решение |
|---|---|---|
| G1 | Диск LXC101 12G | ✅ Увеличен +50G → **59G свободно** (177G total, сервисы active) |
| G2 | Определение tp (нейминг разный) | ✅ Берём tp как у них (по неймингу директолога); НО разный префикс + одинаковый таргетинг = ДУБЛЬ (напр. 2 РСЯ, разница только в картинках) |
| G3 | Rate limits, всё = агентство «Виктори» | ✅ Параллелим по КУКАМ (Grid, N потоков) + 1 токен-поток v5 (Виктори), throttle/backoff (E.3) |
| G4 | Мердж топ-5 → эталон | ✅ Дубль кампании → берём версию с БОЛЬШИМ числом групп |
| G5 | Присвоение gc | ✅ Пока как в текущих слепках (gsheet_naming), ошибки позже |
| G6 | Лимиты Директа на создание | ✅ Не проблема (Семён) |
| G7 | UI-дерево 170 групп | ✅ Нормально будет (Семён) |
| G8 | Гео-остаток в минусах | ✅ Гео-добавки удаляем и из минус-частей |
| G9 | login_key в >1 слепке | ✅ Такие не берём |
| E7 | Медиа | ✅ Сжатые 80%, из 1 аккаунта, дедуп |

## F. Фазы реализации (после сбора)
1. ✅ Гео-чистка (A1, завершена). + ✅ фикс висячего предлога `geo_strip.py` (НЕ задеплоен).
2. ⏳ Движок 1в1 (D2) — фундамент, обратно совместимо. **[в работе — старт 2026-07-14]**
3. ❌ dmp ct+799 — **ИСКЛЮЧЕНО (Семён 2026-07-14: «дмп не трогаем»)**.
4. ⬜ Залить собранное per-group из staging → структура 1в1 (D1, D5), в M3 (D4). ⚠️ M3-проверка (md5 M3==локальное) + cron-safe (Семён). **dmp/gen_ses — вне зоны.**
5. ❌ Восстановить dmp/genses из xlsx — **ИСКЛЮЧЕНО (дмп не трогаем)**.
6. ⏳ two-way синк (D4). **[параллельный трек, инфра]**
7. ⬜ Дополнить пробелы (уточнения/минусы — Codex-аудит перезапустить).
8. ⬜ Деплой + verifier по каждому шагу (правило №2). Только ПОСЛЕ этого — видно в UI «Структура слепков».

### Параллельная раскладка фазы загрузки (2026-07-14, «нужно ускоряться»)
Независимые треки (разные файлы/данные → параллель): **F.2 движок** ‖ **F.6 two-way синк** ‖ **F.4 загрузчик** (dry-run).
Прод-ЗАПИСИ — только после верифицированного дизайна (не вслепую).

### ✅ СТАТУС ФАЗЫ ЗАГРУЗКИ (2026-07-14) — DRY-RUN ГОТОВ, ЖДЁТ ОК НА БОЕВОЕ
- **F.2 движок 1в1** — ✅ реализован (kontent_pack/create_set_text_builders/create_set_tp1_builders/slepki_editor)
  + верифицирован 6/6 (обратно совместим, гейт multi-group, `_group_slug`). **НЕ задеплоен.**
- **Минусы** — ✅ восстановлены (баг `items`→`Items`: 9969→40058 непустых); `_minus_shared` библиотека НЕ трогается
  (правило Семёна «не смешивать»); Yandex-shared-наборов в кабинетах ≈0. **callouts chepelev** восстановлены.
- **F.4 загрузчик** `scratchpad/slepki_harvest_2026-07-14/_build_pack.py` — ✅ DRY-RUN: 119 313 групп → **14 581 групп 1в1**
  (мердж кросс-аккаунт (seg,tp,ct,gk) + дедуп `_копия`). Модельные РСЯ (10575, без ct) — по имени под ct0000 (решение Семёна).
  MANUAL 605 (ЕПК network-search), исключено 449 (НЕ ЗАПУСКАТЬ/шаблон). Выход `_proposed_pack/` (195M) + `_proposed_structure_<slug>.json`.
- **F.6 two-way синк** — ✅ дизайн (pre-push rsync 101→M3 без --delete + tmp-excludes, откат=1 строка). Ждёт ОК + 3 ответа Семёна.
- **Old-naming маппинг** ✅ (`_oldname_map.py`, ~93% автомат). **tp6/7 UAC-скан** ✅ (`_grid_tp67.py`, grid-only детект по 12).
- ⬜ **БОЕВОЕ ПРИМЕНЕНИЕ (ждёт ОК Семёна, hard-to-reverse):** залить `_proposed_pack`→M3 per-group (без клоббера `_minus_shared`,
  md5 M3==локальное, cron-safe) → мердж структуры в per-slepok файлы `direct/slepki/<key>.json` через
  `slepki_store.write_directologists()` (⚠️ superseded 2026-07-17: монолита `slepki_structure.json` больше нет)
  → деплой движка + рестарт direct-create
  (движок+файлы вместе из-за гейта) → verifier по шагам → ТОГДА видно в UI «Структура слепков».
- Добор-шаги после: MANUAL 605, tp6/7-контент (МК-ключи через Grid), media-скачивание (сжатые).
- Трекер/детали: `scratchpad/slepki_harvest_2026-07-14/_PROGRESS.md`.
