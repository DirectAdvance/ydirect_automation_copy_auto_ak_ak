## ⚡ Структура слепков РАЗБИТА по файлам (2026-07-17) — монолита НЕТ
Источник правды — `direct/slepki/<key>.json` (по файлу на слепок) + `direct/slepki/_order.json`.
Монолита `slepki_structure.json` на диске больше НЕТ. ⚡ Загрузка: `slepki_store.assemble()` (единый
dict в памяти, кэш по сигнатуре mtime+size частей). `_json("slepki_structure.json")` перехвачен в
`automation_runtime._json` → `assemble()`, поэтому читатели через `_json` не трогаются.
⚡ ГРАБЛЯ (уже обожглись — create_set_structure/tp1_builders читали монолит → ПУСТОЙ план создания РК):
НЕ читать структуру напрямую с диска (`open(".../slepki_structure.json")`, `read_text`) — вернёт пусто.
Только `_json(...)` ИЛИ `slepki_store.assemble()`. ⚡ Запись: `slepki_store.write_directologists()` —
атомарно per-part (temp+os.replace) только изменившиеся + `_order.json` (маленький git-diff).
⚡ Семейства правил создания: `blueprint_targeting.ruleset_for(slepok)` → `auto`|`custom` (было булево
`auto`; custom = splits/B2B/свои картинки/без числ.гейта/без фильтра марка+модель). UI-группы дропдауна —
поле `ui_group` (`auto`/`auto_other`/`other`), НЕзависимо от ruleset. Онбординг прочего слепка по
скринам — skill `/direct-custom-slepok`.

## ⚡ tp6/tp7 (UAC) — читать ТОЛЬКО через UAC; нули v5/Grid ≠ пусто (моя ошибка 2026-07-17)
Заявил Семёну «tp7 пустые: 0 групп, 0 объявлений» — по v5, потом «подтвердил» по Grid. Семён: «смотри
по кукам, они не пустые». Он прав: внутри было 5 заголовков / 3 текста / 8 сайтлинков / 5 картинок.
⚡ МЕХАНИКА: UAC-кампании (`source="UAC"`, tp6/tp7) **не входят в номенклатуру типов v5**.
`campaigns.get`/`adgroups.get` с явными Ids возвращают `{"Campaigns":[]}` — **пусто БЕЗ ошибки**.
Grid — та же ловушка: `groupsCount="0"`, `hasAds=false`, `groups_for_edit`→0. Т.е. «0 групп»
физически **неотличимо** от «этот API такие кампании не видит». Содержимое живёт в
`/web-api/uac/campaign/{id}` (~167 КБ payload).
⚡ ПРАВИЛО: `source="UAC"` → читать только UAC-эндпоинт. Ноль от v5/Grid по tp6/tp7 — **не
доказательство пустоты**, а артефакт способа чтения. Код это уже знал: докстринг
`grid_read.campaign_content_counts` — «UAC campaigns may legitimately have zero Grid adGroups/ads».
Прочитал бы докстринг вызываемого метода — не подвёл бы Семёна.
⚡ КОНТРОЛЬ-ЗОНД (обязателен перед выводом «пусто»): тем же запросом дёрнуть ЗАВЕДОМО НЕпустой
объект. Здесь: tp1 `712851229` → 1 группа + 2 объявления ⇒ запрос рабочий, дело в природе объекта.
Ноль без контроль-зонда — не факт, а гипотеза.
⚡ КЛАСС ОШИБКИ (второй раз те же грабли — см. «X не существует» ниже): **отрицательный вывод из
инструмента, который физически не может видеть объект.** Первый раз — `curl /yandex.xml` 404 вместо
кабинета. Теперь — v5 вместо UAC. Перед «этого нет» спрашивать: а этот инструмент вообще УМЕЕТ
видеть такой объект? Два согласных нуля из двух слепых API = не подтверждение, а один и тот же слепой пятно.

## ⚡ Рестарт — делать САМ, без спроса (правило Семёна 2026-07-17)
Нужен рестарт сервиса — перезапускать сразу, не спрашивать и не откладывать. **Flask держит Jinja-шаблоны
в памяти: правка ТОЛЬКО шаблона/JS всё равно требует `systemctl restart direct-content`** — иначе страница
отдаёт старый HTML/JS, и проверка «как стало» врёт (поймано 2026-07-17: сортировка очереди «не работала»,
хотя код был верный — сервис отдавал кэш). Правка логики (routes/воркер) → рестарт ОБОИХ:
`direct-content` + `direct-content-worker`. После рестарта — перепроверить фактом, а не по памяти.

## ⚡ «X не существует» — доказывать ПОЛНЫМ списком, не хвостом (урок Семёна 2026-07-17)
Подвёл Семёна: заявил «фидов нет в кабинетах, нужны рабочие /yandex.xml» — а `/yandex.xml` в
`porg-vfdnaolu` БЫЛ (Семён прислал скрин). Три ошибки подряд, все воспроизводимые:
⚡ ГРАБЛЯ №1 (главная): вывод Grid-листинга фидов обрезал `| tail -45` — список первого из трёх
логинов срезался сверху, `yandex.xml` ушёл под обрез. Увидел хвост → решил, что это весь список.
**Отрицательный вывод («нет такого объекта») требует ПОЛНОГО вывода: `wc -l` + счётчик, а не
`tail`/`head`.** Обрезать можно то, что подтверждает наличие, никогда — то, что доказывает отсутствие.
⚡ ГРАБЛЯ №2 (подмена проверки): проверил `curl https://<сайт>/yandex.xml` → 404 → «фида нет».
**Фид — ОБЪЕКТ В КАБИНЕТЕ Директа (Директ скачал его когда-то), а не живой URL.** 404 снаружи не
говорит о наличии фида внутри. Проверять фиды только в кабинете (v5 `feeds` / Grid `Feeds`).
⚡ ГРАБЛЯ №3 (игнор собственного лога): план ещё в первом прогоне писал warning «по одному фиду:
план и создание — только /yandex.xml» = фид НАЙДЕН. Через 20 минут заявил обратное, противореча
своему же логу. Перед выводом «этого нет» — грепнуть свои прошлые логи на упоминание объекта.
⚡ Инстинкт (что означал `UNKNOWN_FIELD: vendor`): НЕ «фид сломан/отсутствует», а «фид ДРУГОГО
ТИПА»: AUTO_RU-фид (`yandex-used-auto.xml`) имеет `mark_id`/`folder_id`, YML/маркет — `vendor`/`model`.
`_resolve_feed_field` при неудачном probe молча отдаёт `synonyms[0]`=`vendor` → Директ 400. Тихий
фолбэк маскирует «я спросил не тот фид» под «данных нет» — читать как СВОЮ ошибку запроса.

## 2026-07-13 · Правило метки tp2-tp5: КС+автотаргетинг при real_keywords>0
Задача: для tp2-tp5 при real_keywords>0 метка обязана содержать автотаргетинг (на поиске ключевые кампании всегда с включённым автотаргетингом), tp1 (РСЯ) не трогать. Фикс: `reconciler_staging/verify_tp15_pull.py::_label` получил параметр `tp`; при `real>0 and tp in (tp2..tp5)` форсит `auto=True`; call-site (~стр.222) передаёт `tp`. ⚡ ГРАБЛЯ (важно, против ложной работы): существующие `verify_tp15_*.json` УЖЕ имели `autotarget=True` на ВСЕХ tp2-5 keyword-кампаниях — потому что в этих кабинетах присутствуют `---autotargeting`-маркеры (auto ставился инцидентно через маркер). → recompute label по новому правилу = **0 изменений** во всех 8 json; запечённое поле `tgt` в slepki_structure.json уже «КС+авто» (59 узлов), plain «КС» на tp2-5 = 0. Фронт `templates/direct/index.html::_TGT_BADGE_MAP` (стр.1503-1511) УЖЕ знает «КС+авто»→cls:manual. Т.е. правка кода = forward-robustness (гарантирует КС+авто даже когда маркер не виден в v5), но данные не переписывал (0 дельты — переписывать identical json/re-bake tgt = бессмысленный клаттер). Синтетик-проверка: `_label(500,False,[],'tp2')`→«КС+автотаргетинг», `_label(500,False,[],'tp1')`→«КС» (tp1 не тронут), `_label(0,True,[],'tp2')`→«автотаргетинг». ⚡ Инстинкт: index.html живёт НЕ в direct/, а в `home/seoadvanced/templates/direct/index.html` (уровнем выше). Файлы: `reconciler_staging/verify_tp15_pull.py`.

## 2026-07-13 · Переименование tp6/tp7 items: AT/manual/hybrid/keywords → русские метки
Задача: привести t-имена МК/ТК во всех слепках к формату «МК автотаргетинг/КС/аудитории/КС+аудитории CPC/CPA». Затронуты: scherbakova/Мультибренд/tp7 (4→3: AT CPA dedup), kuderko/Мультибренд/tp7 (1 rename), kuderko/С пробегом/tp6 (4→2: AT CPA/CRM dedup), kuderko/С пробегом/tp7 (6→2: AT CPA/CRM + hybrid CPA/CRM dedup). Остальные 54 группы нетронуты. ⚡ ГРАБЛЯ (dedup): НЕ применять dedup ко ВСЕМ items группы — он схлопнет «МК - Кредит»+«МК - Общие запросы» (у них targeting_mode=None → одинаковый ключ). Dedup только среди ПЕРЕИМЕНОВАННЫХ items (у которых были English AT/manual/hybrid). Ключ дедупа = (targeting_mode, keyword_source, has_audience). ⚡ Инстинкт hybrid-label: `targeting_mode=hybrid` + `keyword_source=real_corpus` + любой audience_indicator (включая «(нестандарт)»!) → «КС+аудитории»; `keyword_source=None` → «аудитории». «(нестандарт)» НЕ исключать из audience check! ⚡ Инстинкт: json.dumps(indent=2) на slepki_structure.json увеличивает размер с 2.1 MB до 2.9 MB — чисто форматирование, данные те же (проверить через json.load→item_count сравнение). Источник факта: reconciler_staging/<slepok>.tp67.staging.json. scherbakova источник: scherbakova.tp7_restore.v2.staging.json. Бэкап: slepki_structure.json.bak_tp67names_20260713.

## 2026-07-13 · Живая СХЕМА tp1-5 pavlov/piterkina (кампании+группы+targeting из кабинета)
Задача: собрать не presence, а СХЕМУ tp1-5 из живых кабинетов. ⚡ ГРАБЛЯ (реальная конвенция имён pavlov/piterkina — НЕ «Поиск_Автокредит/aon»): имя кампании = `tp{N} {cpc|cpa} {site|kviz} — <CHANNEL> - <TOPIC> - <TARGETING> - <REGION...>`. CHANNEL∈{РСЯ(tp1), Поиск(tp2), «Поиск + Динамика + Товарная галерея»(tp5)}; TOPIC∈{Марки, Модели, Марки и модели, Общее/Общие запросы, Смарт-Баннер, Дилер, Кредит, Автосалон, Б/у авто, Авито, Фид+Каталог, +мультибренд-сплиты «Марки/Модели Lada|Китайцы|Остальные авто»}; TARGETING в ИМЕНИ: Ключевики→keywords, Автотаргетинг→autotarget, Фид/Каталог→feed. РЕГИОН = гео-мультипликатор (одна кампания-шаблон размножена ×N регионов) → при сборе схемы НОРМАЛИЗОВАТЬ регион+cpc/cpa+site/kviz прочь, иначе 67 «кампаний» вместо ~11 шаблонов. ⚡ Инстинкт: targeting кампании — из ИМЕНИ; targeting ГРУППЫ — из кода `ct####_aoff_…—Lada Granta`: aoff=keywords, aon=autotarget; entity=бренд/модель ПОСЛЕ «—». ГРАБЛЯ regex: `\baon\b` НЕ ловит `_aon_` (подчёркивание = word-char) → `(?<![a-z])aon(?![a-z])`. ⚡ ГРАБЛЯ adgroups.get (ИСПРАВЛЕНО 2026-07-13 terehov/gordeeva — это НЕ units): `adgroups.get` принимает МАКС 10 CampaignIds/запрос → аккаунт с >10 tp-кампаний (direct257=22, car-up26=90) отдаёт error «Неверно заданы параметры фильтрации SelectionCriteria» → groups=0 у целого site_type. ФИКС: чанковать CampaignIds по 10. (Ранее ошибочно списано на исчерпание operator-units.) audiencetargets.get — макс 1000 AdGroupIds/запрос. piterkina «С пробегом»: 2 акка, оба camps=1 без tp1-5 → схема пуста (факт кабинета). Файлы: reconciler_staging/facts_scan.py, facts_{pavlov,piterkina,terehov,gordeeva}.json. terehov Мультибренд = эталон tp1-5 (Марки/Модели/Общее/Товарная); tp4 Поиск+Динамика тематическая. gordeeva «С пробегом» — старый Victory-нейминг, tp=0, НЕ собирать. gordeeva-конвенция: дефисы БЕЗ пробелов `РСЯ-Модели-Автотаргетинг` + варианты Ключевики/с ценами/НОВЫЕ/Б-У → сплитить и по дефису между кириллицей.

## 2026-07-13 · Редактор ключей слепков: minus_shared модель + PACK_ROOT = read-only sshfs
Задача: 2-колоночная вёрстка редактора ключей (positive слева / минусы справа) + сделать библиотечные минусы редактируемыми. ⚡ Инстинкт (модель minus_shared): НЕ глобальный — `{slepok}_minus_shared.txt` лежит в keywords каждого ct, префикс `{slepok}_` гарантирует изоляцию слепков. Байт-идентичен по всем ct одного (slepok,site_type,tp) = одна библиотека, размноженная по ct. read_group_keywords (и в slepki_editor.py, и в kontent_pack.py ~1012) мержит `_minus.txt`+`_minus_shared.txt` при чтении. Правка scoped в РЕДАКТИРУЕМЫЙ ct (не размножать на соседние — риск клоббера ct-вариаций + fan-out 200 ct). apply_edit_keywords: писать shared ТОЛЬКО если ключ `minus_shared` в spec (иначе не трогать — обратн. совм.). ⚡ ГРАБЛЯ (важно): `kp.PACK_ROOT=/opt/neuro_kontent` — это READ-ONLY sshfs mount `m3-relay:/Users/Shared/agency/нейродиректолог` (НЕ /opt/neuro_content_local как в docstring). DST-half `_dual_write_pack_file` в него ПАДАЕТ `[Errno 30] Read-only file system`; реально пишет M3-ветка `_ssh_write_m3` на m3-relay, sshfs-mount отражает. Для smoke-теста правок пака как root: writes идут через M3, cleanup junk — тоже ssh m3-relay (не rm на mount — ro). Файлы: slepki_editor.py apply_edit_keywords, routes_slepki_edit.py, templates/direct/index.html (`.sled-kw-cols`, slepkiOpenKeywords/SaveKeywords).

## 2026-07-13 · Живая карта tp1-7 scherbakova/chepelev из кабинета (НЕ Dim_Campaign)
Задача: presence tp1-7 по (директолог×site_type) из ЖИВЫХ аккаунтов. Выборка логинов — ТОЛЬКО public.local_gsheet_sites (все статусы, ≤10 уник login_key на site_type), БЕЗ join Dim_Campaign (недосчёт). tp1-5 = v5 campaigns.get criteria={} (ВСЕ States) + префикс ^tp[1-5]_ в Name; tp6/tp7 = из готовых staging. ⚡ Инстинкт (залипание — ПОДТВЕРЖДЕНО): НЕ звать yandex_gateway.token_for_login для read-карт — он на fallback ПИШЕТ в public.direct_agency_overrides (DB-мутация). Вместо: сам итерируй ygw.direct_tokens().items(), break на первом НЕПУСТОМ; camps=0 достоверно только если все токены дали 0. Отличить «залип» от «пусто»: чужое агентство = error «Объект не найден», управляющее = OK n=0. ⚡ Инстинкт: v5 Client-Login с кириллицей (мусорный login_key «Нет») → latin-1 header encode error → не реальный аккаунт. ВЕРДИКТ scherbakova/С пробегом: 6 логинов, ВСЕ читаются (y-direct-victory OK n=0), 0 кампаний tp1-7 — пустые оболочки (porg-qryhffma: 2 старые «интересы»/«марки» OFF, без tp-префикса). Кабинет подтверждает 0. Файлы: reconciler_staging/live_tp_map.py, live_tp_{scherbakova,chepelev}.json.

## 2026-07-13 · Тон-войс аудит созданных РК (LLM-судья) + отвязанный watcher
Задача: после set-джобы проверять, в ГОЛОСЕ ли слепка сгенерированный контент (не generic-кредит), слать вердикт в личный telegram. Симптом-грабля: контент в `result` джобы НЕ хранится (`slepok_content=None`, `content_source=None`) → читать ТОЛЬКО live из кабинета. ⚡ Инстинкт (v5 read): `yandex_gateway.token_for_login` ЗАЛИПАЕТ на токене с пустым доступом — `v5_get('campaigns',criteria={})` вернул 0 кампаний БЕЗ ключа `error` → функция считает токен рабочим, а он не управляет логином. Для надёжного чтения объявлений ПЕРЕБИРАЙ все `direct_tokens()`, бери тот, что реально отдаёт `Ads` (у меня — max n_ads). v5 лимит `CampaignIds`≤10 (иначе error 4001), поля текста: `v5_get('ads',...,['Id','CampaignId','Type'],extra={'TextAdFieldNames':['Title','Title2','Text']})` → `ad['TextAd']['Title'/'Text']`. ⚡ Инстинкт (Grid ads): запрос ТРЕБУЕТ непустой `filter` (`campaignIdIn`, иначе GridDefectIds.MUST_CONTAIN_NON_NULL_PROPS); типы `GdAdaptiveTextAd{titles bodies}`+`GdTextAd{title body}` — `GdDynamicTextAd`/`GdShoppingAd body` НЕ существуют (UnknownType). ⚡ Инстинкт (голос слепка): `ai_agents.CROSS_SIGNATURE` есть ТОЛЬКО у 5 базовых (pavlov/kryuchkova/scherbakova/terehov/karavaev) — у dmp/gen_ses/стартовых нет → `.get()`, не `[]`. Кампании тестовых наборов ЖИВУТ недолго: `delete_drafts` следующего прогона удаляет черновики предыдущего → историческая джоба даёт read=0 (graceful «не смог проверить»). LLM-судья = `llm_providers._or_complete_url("",messages,...)` (переиспользование, дешёвая `OPENROUTER_LLM_MODEL`); на Маке падает (OR-прокси `127.0.0.1:7891` только на LXC101) → судью гонять на LXC101. Telegram = `loader.send_telegram_message(tok,chat)` (personal). Watcher: НЕ в горячий путь; дедуп `public.direct_tone_checks` (job_id PK) + курсор-файл `.tone_watcher_cursor`=NOW() на 1-м старте (без спама истории); systemd `tone-of-voice-watcher.service` (enabled+active). Верифицировано live: porg-lrfjzcxo(dmp) v5 read 5 камп → score 100 in_voice → telegram sent=true. Файлы: `direct/tools/check_tone_of_voice.py`, `direct/tools/tone_of_voice_watcher.py`, `direct/deploy/tone-of-voice-watcher.service`.

## 2026-07-12 · Аудит structure⊃tp6/tp7 vs profile (karavaev/Монобренд)
Расследование: structure (slepki_structure.json) для karavaev/Монобренд объявляет tp1,tp2,tp3,tp5,tp6(Мастер кампаний,gr «МК»,7 items),tp7(Товарка,gr «ТК»,7 items); profile (targeting_profile.json) — только tp1,tp2,tp3,tp5. Механика гейта: `blueprint_targeting._slepok_profile_excludes_tp` (blueprint_targeting.py:218) — если у слепка ЕСТЬ профиль на site_type, но tp в нём НЕТ → True → tp МОЛЧА режется на генерации (profile АВТОРИТЕТЕН по составу типов, structure держит tp и для донорских целей). Итог = mismatch_type `profile_hides_it`, НЕ deeper_generation_gap (тот требует, чтобы profile ТОЖЕ содержал tp). ⚡ Инстинкт LIVE-детект tp6/tp7 (UAC Мастер/Товарка): они НЕВИДИМЫ в v5 campaigns.get, видны только в Grid. Приём: `grid_list_campaigns(login)` vs `v5_get('campaigns')` — если grid_count==v5_count, значит UAC-кампаний НЕТ (tp6=tp7=0); имена системных tp6/tp7 начинаются с `tp6_`/`tp7_`. Grid typename у ВСЕХ ЕПК = GdUnifiedCampaign (не различает tp) → дискриминатор ТОЛЬКО префикс имени. Грабля запуска ad-hoc на LXC101: сервис = `/root/venv/bin/python3 -m direct.worker_main`, WorkingDirectory `/opt/scripts/home/seoadvanced` → для скрипта ОБЯЗАТЕЛЕН `PYTHONPATH=/opt/scripts/home/seoadvanced` (cwd не в sys.path при запуске файла). Куки Grid только на LXC101 (главпоток+cookies.json) — с Мака Grid не поднять. Karavaev/Монобренд в direct_automation_jobs = ВСЕГО 1 логин porg-asfbs7qe (2 job done, оба total=1); live: 14 РК все DRAFT, префиксы tp1×5,tp2×2,tp4×2,tp5×5 — 0×tp6, 0×tp7 (и 0×tp3, но есть tp4 — отд. аномалия). Файлы: targeting_profile.json, slepki_structure.json, blueprint_targeting.py:218/236, yandex_gateway.py:238(grid_list_campaigns).

## 2026-07-11 · Редактор «Структуры слепков» (edit-джобы очереди, admin)
Задача: сделать вкладку «Структура слепков» редактируемой (ключи/aon-aoff/add-remove ct). Механика (проверено кодом): пул create-воркеров ПАРАЛЛЕЛИТ разные агентства → полный FIFO edit↔create НЕ гарантируется; `slepki_structure.json` читается СВЕЖИМ на каждый `_json()` (blueprint.py:134, БЕЗ кэша) → реальный анти-гонки = АТОМАРНАЯ запись (temp+os.replace), а НЕ очередь. `targeting_profile.json` КЭШИРУЕТСЯ (_btg._TARGETING_PROFILE_CACHE) → после записи ОБЯЗАТЕЛЕН сброс кэша. Пак-ключи: путь `PACK_ROOT/<site_type>/<tp>/<ct>/keywords/<slepok>.txt` (первый уровень = site_type, НЕ Марки/Модели-сегмент — kontent_pack зовёт его `segment`, но callers шлют site_type). Dual-write ОБЯЗАН писать в DST(kp.PACK_ROOT) И в M3-источник(kp.M3_PACK_ROOT via ssh): только-DST сотрёт `_prune_orphans` синка (в RAW файла нет). edit-джоба = `_kind` в теле → воркер-диспетч ветка перед delete_drafts (blueprint.py `_create_worker_loop`), НЕ проходит create-постпроцесс. ⚡ Инстинкт: правишь пак-контент из кода — пиши В ОБА (DST+M3-источник), иначе ночной orphan-prune сотрёт; targeting_profile правишь — сбрасывай _btg-кэш; **[УСТАРЕЛО 2026-07-17: структура разбита по файлам — пиши через `slepki_store.write_directologists()`, читай `assemble()`/`_json`, НЕ монолит; см. верхний инстинкт «Структура слепков РАЗБИТА по файлам»]**. Файлы: `slepki_editor.py`, `routes_slepki_edit.py`, `scripts/slepki_preflight.py:preflight_dict`, `templates/direct/index.html` (renderSlepki/slepki* JS). **✅ LIVE-подтверждён 2026-07-11:** idempotent edit_keywords (karavaev/Монобренд/tp2/ct0000) → job done, dual-write DST+M3 оба ok, аудит в direct_slepki_edits(id=1) И jsonl. ⚡ Инстинкт (грабля отладки): боевые юниты direct-create(.worker) ставят `NEURO_PACK_MOUNT=/opt/neuro_content_local` → PACK_ROOT туда; ad-hoc `python3` БЕЗ этого env берёт sshfs-дефолт `/opt/neuro_kontent` (ДРУГАЯ копия пака!) → при любой ручной проверке редактора/пака ВСЕГДА задавай `NEURO_PACK_MOUNT=/opt/neuro_content_local`, иначе читаешь не ту директорию.

## 2026-07-11 · dmp: реальная структура tp2 (splits) + tp6 МК Авто/Ключи/Конкуренты
Задача: заменить синтетический dmp tp2 (ct0800–ct0821) на реальный из xlsx-экспортов + добавить tp6 с 3 МК. РЕЗУЛЬТАТ: slepki_structure.json dmp/dmp: tp2 → 5 splits (Идентификация-Переходы pay=tcpa / Идентификация-Конверсии pay=cpa / МаркетИнструменты cpa / ОколоТематические cpa / РасширенноеСЯ cpa), 34 группы ct0001–ct0034, 1-кампания-на-split-блок с pay из сплита (create_set_plan.py:494–527); tp6 → группа «МК» из 3 items (МК Авто/МК Ключи/МК Конкуренты), все ct0000. Пак dual-write (LXC101+M3): 34 ct-папки dmp/tp2/ct0001–ct0034/keywords/dmp.txt (1238 ключей из 5 xlsx), dmp/tp2/ct0001–ct0014/keywords/dmp_minus_shared.txt (campaign-minus из Идентификация-Переходы), dmp/tp6/ct0000/keywords/dmp.txt (25 тематических слов МК Ключи). Логин dmp аккаунта: **dmp-ai-450833-qmf4** (из скринов). ⚡ Инстинкт: ct0001–ct0034 у dmp — НЕ конфликтуют с авто-слепками т.к. пак-путь включает segment=dmp (первый уровень); splits-формат обрабатывается create_set_plan.py `_tp2_splits` только если site_type NOT IN _AUTO_SEGMENT_SITE_TYPES. ⚡ Инстинкт (грабля preflight): `scripts/slepki_preflight.py` НЕ понимал nested splits→groups→items → false-positive EMPTY_GROUP. Фикс: добавлен `_all_groups(t)` helper, заменил все 4 вхождения старого паттерна. Файлы: `slepki_structure.json` (dmp/dmp/tp2+tp6), `scripts/slepki_preflight.py` (фикс splits), пак LXC101+M3.

## 2026-07-11 · UI dmp: tp2 «Поиск» = 1 строка НА СПЛИТ (не на группу) в «Создании РК»
Задача: не-авто (dmp) tp2 в «Создании РК» должен показывать РОВНО то, что создаст сервер. Ранняя правка (сделать `_isSegmentTp(2)` false → tp2 падает в generic else-ветку) РАЗДУВАЛА: else рендерит каждую группу ×cpc/cpa (dmp tp2 = 68, tp6 = 6). А сервер для splits-слепка (create_set_plan.py:494-527, `_tp2_splits`) создаёт **1 кампанию на СПЛИТ** (не на группу!): группы = ад-группы ВНУТРИ, БЕЗ cpc/cpa-дубля, pay из самого сплита. Решение: в renderAcSet добавлена ВЫДЕЛЕННАЯ ветка перед generic else (~2032) — `if(tpn===2 && NON_AUTO_SLEPKI.indexOf(agentKey)>=0 && t.splits.length)`: 1 строка-чекбокс на split, header=`split.label`, стратегия по `split.pay` (tcpa→«За клики (Переходы)», cpa→«За конверсии»), группы вложенным `<details>` как ад-группы, счётчик=число сплитов. ⚡ Инстинкт (селект-механика): чекбокс сплита несёт `value=data-desc=split.label`, `data-tp=2` → `selectedPositions().labels` кладёт label → сервер `_sel_labels(2)` фильтрует по label (create_set_plan.py:497,500 — сверяет со `sp.label`); снятие ВСЕХ сплитов → нет checked tp2 → `selectedEngineVariants()` не даёт search_test → tp2 не создаётся (variant-gate). Проверено node+кодом: dmp tp2 = **5 сплитов** (Идентификация-Переходы tcpa/12гр, Идентификация-Конверсии cpa/14, МаркетИнстр cpa/2, Околотематич cpa/2, РасширСЯ cpa/16); scherbakova (auto) не тронут (не в NON_AUTO_SLEPKI → segment-ветка). ⚡ Инстинкт: перед «выровнять UI под N» — СНАЧАЛА посчитать реальный серверный emit (pays-loop, payload_sig), а не верить числу из задачи. Файл: `templates/direct/index.html` (renderAcSet split-ветка ~2032-2060).

## 2026-07-11 · UI dmp: tp6 «Мастер» = 1 строка НА МК-item (не ×cpc/cpa) — вслед за сервером
Задача: сервер ИЗМЕНЁН (Семён, `create_set_plan.py:_emit_struct`) — для НЕ-авто master `_pays_here=(["cpa"] if is_master and site_type not in _AUTO_SEGMENT_SITE_TYPES else pays)` (стр.648): МК больше НЕ дублируются по pays → 1 кампания на МК-item, стратегия cpa. dmp tp6 = 3 (было 6). UI generic else рисовал item×cpc/cpa=6 → рассинхрон. Решение: в renderAcSet добавлена ВЫДЕЛЕННАЯ ветка `if(tpn===6 && NON_AUTO_SLEPKI.indexOf(agentKey)>=0)` (index.html ~2064-2097, перед generic else, зеркало tp2-split-ветки): 1 строка на item из `t.groups[].items[]`, header=item.t, стратегия «За конверсии», счётчик=число item, БЕЗ cpc/cpa-дубля. ⚡ Инстинкт (селект-механика tp6): чекбокс несёт `data-desc=_slepkiPositionName(6,grpName,item.t)` (= «МК - МК Авто») и `data-grp=""` → `selectedPositions().labels` → сервер `sel_pos=_sel_labels(6) or _sel_groups(6)` фильтрует по `g["name"]` из `_slepok_struct_groups` (create_set_context.py:197, display=`grpName - label`). ПРОВЕРЕНО кодом: UI data-desc «МК - МК Авто/Ключи/Конкуренты» 1в1 = серверный g["name"] → выбрать-все=3, выбрать-один=1. checked tp6 → AC_TP_BUILD[6]=[master_auto,master_manual] → want_master → создаётся. dmp ИТОГ: tp2=5 + tp6=3 = **8**, 1в1 с сервером. scherbakova (auto, не в NON_AUTO_SLEPKI, tp6 нет в структуре) — ветка не срабатывает. Файлы: `templates/direct/index.html` (renderAcSet ~2064-2097), сверка `create_set_plan.py:648` + `create_set_context.py:164-202`.

## 2026-07-10 · Grid API: tp6/tp7 ключи из showConditions (грабля GdUnifiedAdGroup)
Задача: получить реальные ключевые фразы Мастер кампаний karavaev для tp67_real_keywords.json. GdUnifiedAdGroup НЕ имеет полей `searchPhrases`/`keywords` (FieldUndefined в обоих). Правильный путь: `showConditions(filter:{typeIn:[KEYWORD],campaignIdIn:[camp_id]}){rowset{...on GdKeyword{id adGroupId keyword}}}` — возвращает фразы для UNIFIED/SMART кампаний через ту же функцию keywords, что и TEXT_CAMPAIGN. Из 3 аккаунтов karavaev собрано: 32 items, 1368 кл, tp6/tp7 × ct0006/ct0014/ct0111/ct0115/ct0119/ct0120/ct0319 (7 brand ct каждый). master_at кампании = 0 ключей (ожидаемо, авто-подбор). Файл: `home/seoadvanced/direct/tp67_real_keywords.json`. ⚡ Инстинкт: для Мастер кампаний keywords → ТОЛЬКО showConditions с typeIn=[KEYWORD], НЕ adGroups(rowset{...on GdUnifiedAdGroup}) — там этих полей нет.

## 2026-07-10 · content-editor: code-review фиксы reorder (5 находок)
Правки `routes_content_editor.py _reorder_sitelinks`/`ce_replace` + `content_editor.html ceReorderApply`. ⚡ Инстинкт (ГЛАВНОЕ): reorder — ПОЗИЦИОННАЯ операция, а превью/`RO_N` строятся ТОЛЬКО по `CE.content` = загруженный `CE.login` (1 логин). Поэтому применять СТРОГО к `[CE.login]`, НЕ к `ceTargetLogins()` (мультивыборка): на непросмотренных акках иной состав наборов → слепая перестановка. ⚡ Инстинкт: порядок/идентичность быстрой ссылки сверять по ПОЛНОМУ кортежу `(title,href,description)` (`_sl_tuple`), НЕ по title — swap ссылок с одинаковым title/разным href иначе даёт ложный «read-back не подтвердил»/«без изменений». ⚡ Инстинкт: элемент набора быстрых ссылок = РОВНО {title,href,description} (v5 sitelinks.get / Grid get_sitelink_sets / add_sitelink_set — одна тройка; per-item `id` серверный, на создании не шлётся) → перестановка снимка полей не теряет. #3 счётчик reorder: `replaced==applied_sets` (наборы), детали отдельно `campaigns_touched`/`ads_touched`/`uac_sets` (не смешивать единицы). #4 sync `ce_replace` тоже должен 400 на substring+не-AD (как preview/replace_async). LIVE porg-psm5h7q6 OFF: UAC swap→applied(uac_sets=1)→revert byte-for-byte main-link scalar unchanged; campaign campaigns_touched=1→revert дедуп 1492751576; ad ResponsiveAd honest skip. ⚡ Инстинкт standalone-репро: `sys.path.insert(0,"/opt/scripts/home/seoadvanced");sys.path.insert(0,"/opt/scripts/.secret")` + scp→pct push /tmp→`/root/venv/bin/python3` из cwd `/opt/scripts/home/seoadvanced`; полный Grid-load акка = минуты → фон+poll.

## 2026-07-10 · content-editor: фича sitelink_reorder (позиционная перестановка порядка быстрых ссылок)
Задача: массовая перестановка ПОРЯДКА быстрых ссылок (drag-and-drop, permutation по индексам) во всех кампаниях выбранных аккаунтов. Backend `routes_content_editor.py`: `_validate_permutation` (биекция 0..N-1, N≥2, не тождество), `_reorder_sitelinks` (ядро — применяет perm к КАЖДОМУ набору content["sitelinks"], per-set report applied/skipped/error, result: replaced/applied_sets/skipped_sets/reports), ветка `sitelink_reorder` в `_do_replace` (perm лежит JSON в new_text), `_SITELINK_JOB_TYPES=_SITELINK_TYPES|{"sitelink_reorder"}` (executor `_need_cl_sitelinks` грузит campaign-level наборы для reorder), эндпоинт `/api/content-editor/sitelinks/reorder_async` (та же очередь content_jobs, type='sitelink_reorder', mode='reorder', old_text=new_text=json(perm)). Пути записи ПЕРЕИСПОЛЬЗОВАНЫ (не дублировал): UAC→`_uac_patch_campaign_texts(...,"sitelinks",reordered)` (осн.ссылку href НЕ трогаем, перечитываем реальный массив detail для byte-safe); campaign-level→`add_sitelink_set(reordered)`+`set_campaign_sitelink_set`; ad-level TextAd/DynamicTextAd→`add_sitelink_set`+`_v5_rebind_ads_sitelink_set`; ad-level ResponsiveAd→честный skip; наборы < N→skip с отчётом. Frontend `content_editor.html`: кнопка «↕️ Порядок быстрых ссылок» (видна только в разделе sitelinks, тоггл в ceSetSection), панель ce-reorder-panel с drag-and-drop чипами (HTML5 dragstart/drop, RO_ORDER=массив исходных индексов), селектор N=самая частая длина наборов, клиентское превью было→стало per-set из CE.content.sitelinks. LIVE-ВЕРИФ porg-psm5h7q6 (всё OFF, swap [1,0,2,3,4,5,6,7], возврат): UAC 712694743 swap→revert byte-for-byte, **осн.ссылка href /auto UNCHANGED**; campaign-level 1492751576→новый 1492769958→revert Grid-дедупом на исходный 1492751576 identical; ad-level ResponsiveAd 1492662343→**skipped «не поддерживается» replaced=0, набор не тронут**. ⚡ Инстинкт (ГЛАВНОЕ): swap двух позиций — ИНВОЛЮЦИЯ → revert = повторный тот же perm; для RK дедуп `add_sitelink_set` при идентичном содержимом возвращает ИСХОДНЫЙ set_id (бесплатный откат, orphan-набора нет). ⚡ Инстинкт: UAC ТОВАРКА tp7 → partial-PATCH `{sitelinks}` ОТВЕРГАЕТСЯ → `_uac_patch_campaign_texts` шлёт FULL payload → UAC на save РЕОРДЕРИТ device_types/ad_group_briefs (мембершип цел, семантически инертно, main link href цел) — ПРЕД-СУЩЕСТВУЮЩЕЕ поведение общего UAC-write-пути (те же href/title замены), НЕ регресс. ⚡ Инстинкт: standalone-репро write-путей редактора на LXC101 — положить скрипт `direct/_tmp_*.py` в MUTAGEN-синкаемый tree (pct push из scratchpad НЕ работает), запуск `/root/venv/bin/python3 -m direct._tmp_X` из cwd `/opt/scripts/home/seoadvanced`; Grid-load всего аккаунта МЕДЛЕННЫЙ (минуты) → фоновый прогон + poll. ERRORS_JOURNAL: CONTENT_EDITOR_SITELINK_REORDER ✅.

## 2026-07-10 · content-editor: стрей-пробел во фрагменте → лишний пробел бренд↔фрагмент
Задача: mode=substring, «Стало — фрагмент» со случайным ведущим ПРОБЕЛОМ превращал `Jetour2026` в `Jetour 2026`. Корень: обычный пробел `str.strip()`/JS `.trim()` УЖЕ чистили (этот кейс работал), но НЕвидимые/zero-width (BOM U+FEFF, ZWSP U+200B, ZWNJ/ZWJ, word-joiner U+2060) `.strip()` НЕ убирает (`'﻿'.isspace()==False`), JS `\s` не покрывает U+200B..U+200D/2060 → символ вставлялся между брендом и фрагментом, и сбивал гард `len(new)>len(old)`. Фикс: `_frag_trim()` в `routes_content_editor.py` (`.strip()` + `.strip(_FRAG_INVISIBLE)` + `.strip()`) + JS-двойник `ceFragTrim()` в `templates/direct/content_editor.html`; заменил ВСЕ нормализации old_text/new_text и все length-гарды (preview/replace/replace_async/`_do_replace`). Внутренние пробелы целы (strip только по краям). Верификация LXC101 реальным `_match_targets`: plain/NBSP/BOM/ZWSP/word-joiner/trailing → все чистый `Jetour2026`; before-fix BOM/ZW вставляли символ; exact-режим цел (hit=1/non-match=0). ⚡ Инстинкт (ГЛАВНОЕ): `.strip()`/JS `\s`/`.trim()` НЕ трогают zero-width (U+200B..U+200D, U+2060) — для пользовательских фрагментов нужен явный набор невидимых. ⚡ Инстинкт: правка ЛОГИКИ фрагмент-замены → рестарт ОБОИХ (`direct-content` превью + `direct-content-worker` запись), иначе превью и запись разъедутся. ERRORS_JOURNAL: CONTENT_EDITOR_FRAGMENT_STRAY_WHITESPACE ✅.

## 2026-07-10 · Phase1: rebuild slepki_structure из корпуса (scherbakova/terehov/pavlov/kryuchkova)
Задача: баг «все слепки дают одну структуру» — выяснилось что gordeeva=kuderko=**terehov**=zubakin идентичны (tp1/tp2/tp5). Среди 4 целевых (sch/ter/pav/kry) коллизий НЕ БЫЛО — они уже уникальны между собой. Корень: terehov был дублем gordeeva/kuderko/zubakin, а не других 3 целевых. Итог пересборки: scherbakova/pavlov/kryuchkova: Δ=0 (их ct4 все подтверждены корпусом), **terehov: -347 items** (tp6: -279 + tp7: -23 — эти типы он не ведёт в корпусе; tp1/tp2/tp5: -13 brands не в corpus). Метод: scan adgroups.jsonl всех login-папок → ct4-set (порог ≥1), scan campaigns.jsonl → used_tps. Фильтр: tp not in used_tps → empty; ct4==ct0000 → keep; ct4 not in corpus_ct4 → remove. Staging: `slepki_structure.staging.json` (НЕ боевой). Генератор: `scripts/build_slepok_structure.py`. Отчёт: `scratchpad/phase1_rebuild_report.md`. ⚡ Инстинкт: перед стартом rebuild проверить fingerprint СРЕДИ 4 целевых — возможно коллизия только с NON-target и 3 из 4 уже корректны. ⚡ Инстинкт: corpus ct4 (182-209 кодов) может полностью покрывать superset JSON items (247 кодов общего суперсета, но у каждого dir своя выборка) → Δ=0 — это не баг скрипта, а подтверждение корректности. Файл: `scripts/build_slepok_structure.py`.

## 2026-07-10 · Профиль таргетинга chepelev (Чепелев Никита)
Задача: собрать targeting_profile для слепка chepelev из 36 аккаунтов (DB: direction=Авто). Результат: 23 ok, 8 = 8800, 3 пустых, 2 error. site_types_covered: Квиз, Монобренд, Мультибренд. tp6/tp7 = 0 (нет UNIFIED/SMART). Ключевые находки: (1) Именование кампаний — строгий стандарт `tpN_cpc_{site|kviz} – {РСЯ/Поиск} – {Марка/Модели/Товарная} – [Ключи/]Автотаргет – {регион}`; tp-код ВСЕГДА в имени как префикс → классифицировать через re.match(r'^tp(\d+)[_-]'). (2) `_cpc_kviz_` кампании внутри Мультибренд/Монобренд = site_type «Квиз» (2 реальных Квиз-аккаунта оба недоступны — 1 error, 1 8800). (3) ВСЕГДА смешанный режим: каждая группа содержит И AT И КС одновременно (отличие от других директологов, у которых группы либо AT-only либо КС-only). (4) tp4 у Чепелева = TEXT_CAMPAIGN с именем «tp4_cpc_site – Поиск» (НЕ DYNAMIC_TEXT_CAMPAIGN); в нейродиректолог-системе это ближе к tp2. (5) «Товарная» в именах кампаний = маппинг в сегмент «Фиды». (6) tp3 присутствует в Мультибренд (малый объём — 23 группы). Файл: `scratchpad/chepelev_profile_analysis.json`. Скрипт: `scratchpad/harvest_chepelev_v2.py`. ⚡ Инстинкт: при классификации tp кампаний Чепелева — читать ПРЕФИКС имени (tpN_), не слова «рся»/«поиск» (старые аккаунты типа «autopark Китай» не несут этих слов, но несут tp1_ префикс). ⚡ Инстинкт: «Марка/Модели» в имени кампании = ОБА сегмента одновременно (count в оба).

## 2026-07-10 · content-editor: быстрые ссылки бывают на 2 уровнях (кампания/объявление)
Задача: смена title/description быстрой ссылки падала для наборов уровня КАМПАНИИ («заменён 1/1, ошибок 80» / «не подтвердилась у N объявлений»). ⚡ Инстинкт (ГЛАВНОЕ): быстрые ссылки в ЕПК бывают campaign-level (`inheritableSitelinkSet`) И ad-level (`SitelinkSetId`). **v5 ads.get НЕ отдаёт унаследованный campaign-level набор — у наследующих объявлений `SitelinkSetId` ПУСТ** (диагностика porg-psm5h7q6: `ads_carrying_inh=0` во всех кампаниях). Поэтому `_load_account` (собирал наборы ТОЛЬКО из ad-level) не видел campaign-level наборы → замена целилась в пустой ad_ids → Grid не применял. Фикс: `_load_account` читает набор каждой кампании через `grid_finalize._read_unified_campaign_update_payloads(cids)` → `inheritableSitelinkSet.sitelinkSetId` → добавляет campaign-usage (`adgroup_id=0`) в `sitelink_usages` + `campaign_ids_by_set` + `level`/`campaign_ids` в sitelinks_out. Запись НЕ трогал — `_replace_sitelink_text_grid` УЖЕ умеет campaign-level (`add_sitelink_set`+`set_campaign_sitelink_set`), просто его не кормили campaign-usages (корень был в ЧТЕНИИ, не записи). Ad-level не сломан: гейт `current_set_by_campaign.get(cid)==source_set_id` фильтрует ad-level наборы в пустой campaign_ids → ad-путь как раньше. ⚡ Инстинкт: v5 `sitelinks.get` по Id читает и campaign-level наборы с полным содержимым (fallback на Grid `get_sitelink_sets` не нужен). ⚡ Инстинкт: Grid при перепривязке через identical-content набор переиспользует существующий set-id (при revert контент восстанавливается без orphan-наборов). Верификация live: title+description replace → replaced=1/errors=[]/read-back OK, откат байт-в-байт. ERRORS_JOURNAL: CONTENT_EDITOR_SITELINK_CAMPAIGN_LEVEL_INVISIBLE ✅. Стандартный способ standalone-репро: `from direct import blueprint as bp; token,_=bp._token_for_login(login,"",bp._direct_tokens()); bp._v5_call(...)` + `from direct.grid_finalize import GridClient`.

## 2026-07-10 · Харвест karavaev: реальная структура аккаунтов (source of truth)
Задача: собрать РЕАЛЬНУЮ структуру слепка karavaev из 67 его аккаунтов в Victory DB. Результат: 25 аккаунтов с данными, 42 = 8800 (реально удалены на Яндексе), 0 tp6/tp7 нигде. TP: TEXT tp1=558/tp2=243/tp3+tp5+tp4=83, UNIFIED=0, SMART=0. РЕАЛЬНАЯ СТРУКТУРА: (1) tp2 Поиск = ОТДЕЛЬНАЯ кампания на каждую марку ("Поиск - Haval/Changan/Chery/…" — 13 брендов у Москвы); "Поиск Китай" суффикс = Китайские модели. (2) tp1 RSY = "РСЯ Модели АТ" (AT=True/KW=False, все 5 категорий) + "РСЯ Модели Ключи" (KW=True) + "РСЯ Марки АТ/КС" + "Смарт-Баннер/Фиды". НЕТ отдельных РСЯ на автокредит/салон. (3) tp3 Товарная галерея и tp5 Поиск+Динамика+ТГ — есть у Карав. (4) tp6/tp7 ОТСУТСТВУЮТ — это НОВАЯ целевая структура. Текущий слепок (ct0000-ct0014 группы) = НЕ его структура, нужен полный редизайн. Файлы: `/private/tmp/.../scratchpad/karavaev_real_structure.json` + `karavaev_analysis.json`. ⚡ Инстинкт: Mac SSL verify=False на requests.post иногда даёт SSLEOFError — фикс: создать requests.Session() + HTTPAdapter с urllib3.create_urllib3_context(check_hostname=False). ⚡ Инстинкт: campaigns.get БЕЗ SelectionCriteria.States возвращает ВСЕ типы TEXT_CAMPAIGN включая архивные; UNIFIED/SMART = 0 если их реально нет (не артефакт API). Группы tp2 Карав: несколько групп на модель с суффиксами intent (Купить/Новый/Салон/Дилер + конкретные модели по ct-коду).

## 2026-07-10 · content-editor: ДВА сервиса + модал confirm + Title2 используется
Задача: заменить браузерный confirm() на центрированный модал в `templates/direct/content_editor.html`; проверить нужна ли опция «Заголовок 2» в массовой замене фрагмента. Сделал: JS-функция `ceConfirm(message, opts)` → Promise<boolean> (строит из готовых .ce-modal-* классов, Esc=отмена/Enter=OK/клик по фону=отмена/фокус OK), заменил все 4 `confirm()` на `await ceConfirm()` (ceApply, ceFragApply, _pcJobAction, pcRecalcNow — все async). Грамматика ceFragApply: «во всех заголовках/текстах» (предложный, отдельная whereWord) вместо `ceFragTypeWord` (родит. — оставлен для превью). node --check извлечённого JS = OK. ⚡ Инстинкт (ГЛАВНОЕ): редактор контента `/direct/automation/content` = ДВА сервиса — `direct-content.service` (:5021, flask/UI/шаблон/preview, `routes_content_editor.py`) И `direct-content-worker.service` (очередь `direct_automation.content_jobs`, применяет замены). Правка ЛОГИКИ (routes/воркер) → рестарт ОБОИХ; иначе воркер крутит старый код (инцидент: `mode=substring` упал «текст не найден» т.к. воркер игнорил новый mode → exact-match целого заголовка). Правка только шаблона/JS → достаточно `direct-content`. Колонка `mode` в content_jobs = миграция `ensure_jobs_table()` при старте flask. Рестарт: `ssh proxmox-ts "pct exec 101 -- systemctl restart <unit>"` (прямой ssh lxc101 не отвечал). ⚡ Инстинкт: Title2 (API-поле «n») РЕАЛЬНО генерится и всегда заполнен — `create_set_text_builders.py`/`create_set_tp1_builders.py`: `ttl2 = ai_title2[:30] if ai_title2 else n` (ИИ или round-robin из пула `public.direct_title2` через `_next_title2`), `campaign.py: text_ad["n"]=title2`. Опцию «Заголовок 2» НЕ убирал. Записано в ERRORS_JOURNAL (CONTENT_EDITOR_TWO_SERVICES_WORKER_STALE).

## 2026-07-10 · kryuchkova: дозаполнение обрезанных ct в Квиз/Монобренд/Мультибренд tp1
Задача: сэмпл-диагностика нашла ~7 ct-кодов с 1-3 строками (model-name-only) в кryuchkova.txt. Проверка reality: ct0034 (Changan CS75 FL) = 2 строки в паке, 12 distinct CS75 keyword в fact_criterion_spend_marka_kupit → ОБРЕЗАН (≥5 → дозаполнить). Остальные ct: ct0032 (CS55, 3 строки, 3 реальных keywords), ct0073/ct0074 (Dongfeng Huge/Mage, 1 строка, 1 ключ=имя модели), ct0246/ct0247/ct0248 (XCITE 3 кода, 1 строка, 1 ключ=имя модели) — ЛЕГИТ-ТОНКИЕ (реал ≤4 кл → не трогать). Дозаполнено: 3 файла ct0034 (Квиз, Монобренд, Мультибренд)/tp1: 2 строки → 12 реальных CS75 ключей Крючковой (чанган 75, changan 75, cs 75 plus changan, …). Оба носителя LXC101 + M3. Бэкапы .bak (2 строки). ⚡ Инстинкт: «Changan CS75 FL» в pack = ct0034; реальные ключи Крючковой для этого ct = CS75 Plus (не FL-specific, FL-ключей 0 impressions) — это норма для RSY pack. ⚡ Инстинкт: Dongfeng Huge/Mage и XCITE X-CROSS — легит 1-строчные файлы (директолог крутит 1 ключ=модельное имя); путать с обрезанием не надо.

## 2026-07-10 · salamahin: тематик-баг ct0010/ct0014 + полнота ct0150 (Jetta)
Задача: аудит флагнул 3 файла salamahin. Тематик-баг: tp1/ct0010 (21 строка Changan Alsvin) + tp2/ct0010 (33 строки Changan Alsvin) + tp1/ct0014 (228 строк, 6 Changan CS75 Plus смешаны с общим авто) — все РЕАЛЬНЫЕ БАГИ. Фикс ct0010 x2: cp zubakin.txt (98 строк, геонейтральный Дром, no «Новгород»). Фикс ct0014 tp1: cp tp5/ct0014/salamahin.txt (303 строки, чистый, гео Краснодар/Волгоград). Полнота ct0150 (Jetta Казань): tp2 (11 строк) = обрезан — vs5/vs7 в Казани есть в fact_criterion_spend_marka_kupit (4 критерия), нет в паке → расширено до 30 строк. tp1 (179 строк) = НЕВЕРНЫЙ — содержал KGM/SsangYong ключи (Екатеринбург) смешанные с Jetta + склеенная строка «стоимость jettajetta Омск» → убрали KGM, починили склейку, добавили vs5/vs7 → 46 строк. ct0066 (Dongfeng Казань, 16 строк): норма — 0 Казань-критериев Dongfeng в fact_criterion, 159 в других городах. ct0108/ct0242: норма. Оба носителя: LXC101 + M3. Бэкапы .bak. ⚡ Инстинкт: при аудите тонких файлов через fact_criterion_spend_marka_kupit — фильтруй по ГОРОДУ критерия (ILIKE '%казан%'), не только по бренду: 159 Dongfeng across all = не означает 159 для Казань-клиента. ⚡ Инстинкт: tp1 ct файлы могут содержать смешанный контент разных брендов (баг генерации) — при проверке "правильности" grep по ct-бренду и считай "чужие" строки.

## 2026-07-10 · pavlov Мультибренд: tp1 «группы не созданы» = транзиент API; tp2/ct0014 Geely-фикс
Задача: прогон 2240a4d1143a упал «tp1 не дозаполнена: группы не созданы» (20/21). Корень: пак ПОЛНЫЙ (189 pavlov tp1 ct на M3+LXC101 с положительными ключами, gather вернул 190 ct). Упала «РСЯ - Марки - Автотаргетинг» (campaign_id=712695456): adgroups.add API вернул ошибки → rep["adgroups"]=0. Не путать: «пак пуст» = skipped="пак пуст:..." (create_set_tp1_builders:750-754); «группы не созданы» = skipped=None+error=None+adgroups=0 = транзиент API (сосед «Марки-КС» с тем же паком прошёл). ⚡ Инстинкт: «группы не созданы» без пустого пака = транзиент adgroups.add, повторный прогон лечит без правки пака. Также обнаружено загрязнение: tp2/ct0014 содержало 79 строк Geely = 23 «geely» (латиница) + 30 «джили» (кириллица = та же марка!) + 6 гео (Новосибирск/Сургут) + 5 бу + 6 нейтральных-дилерских. Grep -v -i "geely|джили|..." → 834→755 строк. Оба носителя: LXC101 + M3 (ssh m3-relay с LXC101). ⚡ Инстинкт: при счёте «N geely строк» проверяй кириллицу «джили» — русское написание Geely, не попадёт в latin-only grep и может удвоить счёт загрязнения.

## 2026-07-10 · Слепок karavaev Мультибренд: tp6 ct0115/ct0120 (Haval H3/M6) + ct0010/ct0014 тематик-фикс
Задача: 2 предупреждения «tp6 КС без ключей slepok=karavaev ct=ct0115/ct0120 → fallback autotarget». Механика: create_set_plan.py:629 `_ct_for_name(group_name)` сопоставляет имя группы «Haval H3»→ct0115 / «Haval M6»→ct0120, план пишет `ct=ct0115` в item, `_tp67_keywords_for` ищет `kontent_oktyabr/Мультибренд/tp6/ct0115/keywords/karavaev.txt` — файла нет → fallback autotarget. Лечение: создал директории ct0115/ct0120 в tp6 и заполнил karavaev.txt (18/19 тематических слов формата «haval h3 2025», «хавал н3», «купить хавал m6»). Модели реальные у karavaev — подтверждено tp2 ct0115/ct0120 с богатыми keywords. Также исправлены ошибки тематик-ct Мультибренд/tp2: ct0010 (Дром) содержал ситирей keywords → заменён на tp1/ct0010 Дром-запросы (149 строк); ct0014 (Общ.авто) содержал 180 строк monjaro + 200 строк general → grep -iv monjaro → 200 строк general. Все остальные ct0014 (tp1/tp3/tp5 Мультибренд, Монобренд tp1/tp5) чисты. Оба носителя: LXC101 `/opt/neuro_content_local/` + M3 `/Users/Shared/agency/нейродиректолог/` (через ssh m3-relay). Backup *.bak на обоих. ⚡ Инстинкт: tp6 КС-позиции с именованными брендами (Haval H3 ≠ ct0000 в code!) → create_set_plan.py:629 `_ct_for_name(group_name)` даст ct0115, а НЕ ct0000 — нужен файл в tp6/ct0115/ а не tp6/ct0000/.

## 2026-07-10 · Слепок scherbakova: ct0010/ct0014 тематические ключи — массовый фикс
Задача: ct0010 (Дром) и ct0014 (авто-общее) во всех сайт-типах кроме Мультибренд содержали МОДЕЛЬНЫЕ ключи (cityray/monjaro). Аффект: 4 сегмента (Квиз/Монобренд/Мульти+БУ/С пробегом) × tp2+tp5 × ct0010+ct0014 = 16 файлов. tp1 был чистым. Фикс: cp src.bak → cp Мультибренд/tp2/ctXXXX → в место назначения; шаблоны tp2 и tp5 одинаковые (177/187 строк). Оба носителя: LXC101 `/opt/neuro_content_local/kontent_oktyabr/` + M3 `/Users/Shared/agency/нейродиректолог/kontent_oktyabr/` (через `ssh m3-relay` с LXC101). Модельные ключи cityray → ct0100, monjaro → ct0104 (Мультибренд) НЕ тронуты. ⚡ Инстинкт: при пак-фиксе на нескольких сегментах — писать bash-скрипт, заливать через `scp proxmox-ts:/tmp/ && pct push 101`, не пытаться через многострочный `sh -c` в `pct exec` (квотирование сломает цикл). M3 всегда через `ssh m3-relay` с LXC101.

## 2026-07-10 · R2-3: детерминированный brand-first reorder на создании + короткие
Задача: гарантировать brand-first (марка до первой точки) для tp1/tp2/tp4 на СОЗДАНИИ, не надеясь на LLM. КОРЕНЬ «reorder не работал live»: **`_upgrade_credit_titles` (create_set_assets.py) — ФИНАЛЬНАЯ сборка Titles на TOKEN-пути** (`_responsive_ad`, tp1 `create_set_tp1_builders:328`, tp2/tp4 `create_set_text_builders:127`) — при `_needs_credit_title_upgrade=True` ЗАМЕНЯЛ brand-first g_titles своими вариантами `f"Кредит на {anchor}…"`/`f"Платеж … {anchor}"` (марка ПОСЛЕ УТП + короткие ~43, без `_fill_title`) = ТОЧНО симптомы FACT. Прежний reorder в `_rsya_titles` давал brand-first g_titles, но upgrade клобберил их ПОСЛЕ. Cookie-путь (grid `build_ad._fill_titles`) upgrade НЕ зовёт → клобберил только token (прогон 40М баллов = token). Фикс детерминированно: (1) `text_gen._brand_first_reorder(title, brand)` — сегментный реордер марки в начало (guard: реордер→bad_ad_title/is_bad_start → откат на оригинал; brand пуст/Общее/ct0000 → no-op; марки нет → как есть); (2) `_rsya_titles` (choke-point ОБОИХ путей для group-titles tp1/tp2/tp4) применяет реордер в главном цикле + brand_fillers → g_titles всегда brand-first; шаблон `_brand_title_set` «Кредит на {brand}» → «{brand}. Выгода…»; (3) `_upgrade_credit_titles` варианты brand-first + `_fill_title`(≥54) + реордер pass-through seq. Backup `fix_brand_not_first`→delayed content_repair (blueprint:7979) реально доедет после R2-1/К1 фикса фриза. Верифай изол.тестами (нет Flask локально → text_gen.configure стабами): BAIC/Haval/Chery 0 нарушений brand_head_ok И lead, len 48-56; `_responsive_ad` token-финал 0/7; Общее-ветка валидна; py_compile+pyflakes(0 новых undefined). НЕ деплоено, live round 2. ⚡ инстинкт: заголовки объявлений собираются в ДВЕ стадии — group-level `_rsya_titles` И финал-level (`_responsive_ad`→`_upgrade_credit_titles` token / grid `build_ad._fill_titles` cookie). Правишь контент заголовков — проверяй ОБЕ стадии: `_upgrade_credit_titles` может ПЕРЕЗАПИСАТЬ хороший вход своими вариантами (только token-путь). `_responsive_ad` НЕ знает brand → реордер по heuristic anchor из `_credit_title_anchor`.

## 2026-07-09 · F: async Grid-финализация под флагом DIRECT_ASYNC_FINALIZE (дефолт OFF)
Задача: вынести медленную campaign-level Grid-финализацию (`_finalize_rsya`/`_finalize_search_via_grid` — места показа/инварианты/ассеты-attach/минус/bidmods) из синхронного цикла создания в АСИНХ-очередь. **Очередь — ПЕРЕИСПОЛЬЗОВАЛ `direct_delayed_repairs` (kind='finalize_set')**: там уже есть демон (`_delayed_repair_daemon_loop`), watchdog, reschedule-до-нуля/attempts-cap, дедуп `ON CONFLICT(parent_job_id,kind)`, интеграция с карточкой (`_parent_absorb_child_start/_progress`). Новая таблица НЕ нужна (лестница минимализма). Новый модуль `create_set_finalize_queue.py` (Flask-free, configure(deps)). **Ключевой инсайт — маршрутизация захвата ПО login** (первый позиционный arg финализации): один login за раз = один воркер (agency-gate+login-dedup), поэтому глобальный реестр `{login: recorder}` корректен и для C1-параллельных каналов (обе нити зовут финализацию с тем же login), и для пула воркеров — **thread-local/contextvars НЕ годятся (не пробрасываются в под-нити каналов)**. Спеки финализации полностью JSON-сериализуемы (login/cid/name/goal/cpa/counter/platforms-dict/callout_ids/…) → `run_finalize_job` = REPLAY тех же функций с теми же args/kwargs = «ровно тот же набор Grid-операций», вне цикла. Единственный seam перехвата на ВСЕ билдеры (feed/gallery/text/tp1) — **две blueprint-обёртки** `_finalize_rsya`/`_finalize_search_via_grid` (инжектятся во все builder-deps): `capture_finalize` → OFF False мгновенно (инлайн байт-в-байт) / ON+окно захват+skip+return []. **Грабля: replay должен биндить РЕАЛЬНЫЕ csfin-функции, НЕ blueprint-обёртки — иначе воркер снова захватит и зациклит skip** (проверил: `_DEPS["finalize_search_via_grid"].__module__ == direct.create_set_finalize`). DoD-семантика на card-level (авторитетно, не per-result-флаг): пока finalize-джоба не done → карточка running (child `fin:{did}` через `_parent_absorb_child_start`) + `result['finalize_pending']` → summary НЕ зелёный; терминал → child закрыт+pending снят → зелёный. worker: register окна перед созданием, enqueue после done, **unregister в finally** (анти-leak при error/cancel). Верифай статикой: py_compile Mac+LXC, pyflakes чисто, import blueprint LXC, изолированный trace OFF/ON/replay/partial/str-result зелёный, сервисы active, флаг НЕ в unit-env→прод OFF. **Живьём НЕ прогонял (C2, porg-psm5h7q6)**; сервис не рестартовал (правило: рестарт в середине набора сиротит resumed). Осталось: захват покрывает 2 finalize-функции (главный тормоз); adPrice/автотаргет/shopping — пока инлайн (тот же паттерн capture-по-login если надо). ⚡ инстинкт: перехват кросс-модульного шага без job-контекста — **маршрутизируй по естественному ключу из сигнатуры (login), не по thread-local**; replay-функции всегда бинди в обход capture-обёрток.

## 2026-07-09 · B (API-first): tp2/tp4 на token/баллы под флагом DIRECT_API_FIRST (дефолт OFF)
Задача: у агентства много баллов → tp2/tp4 создавать ТОКЕНОМ (API), не кукой; фолбэк на куку по 152; строже флип 152. Ключевое: **token-билдер `_build_text_from_pack`/`_build_tp2_from_pack` (create_set_text_builders) ПРИНИМАЕТ campaign_id — shell'а НЕ создаёт**; и `_build_tp2_adgroups` льёт группы через **v5 `adgroups.add` → relevanceMatch НЕ ставится** (в отличие от cookie-пути `gc.create_full`→`build_adgroup(autotargeting_profile="search_tp2")` и tp5 `_build_tp1_adgroups`). Поэтому DoD-паритет требует ДВА добора после `_build_text_from_pack`: (1) shell = `_create_search_test_campaign(mode="search", pay)` (TEXT_CAMPAIGN, инварианты #3/#4/#5+ENABLE_COMPANY_INFO=NO); (2) relevanceMatch EXACT_V2_MARK+WITHOUT_BRAND через Grid `groups_for_edit`+`build_update_item`+`update_unified_adgroups` (тот же примитив, что `repair_executor.execute_keywords_repair`); (3) `_finalize_search_via_grid(platforms=_search_platforms(tp_code))` (places SEARCH_PAGE, organic tp4-only, ассеты, инварианты) — те же args, что cookie; (4) campaign-минус `_apply_campaign_direct_minus`=`_enabled_minus_words` (v5 NegativeKeywords, эквивалент cookie-spec `minus_keywords`). Всё в новом `_create_text_via_token` (create_set_feed_builders, рядом с `_create_text_via_cookie`, **cookie-путь НЕ трогал** ради OFF-байт-идентичности). Роутинг в `run_create_set_text`: `_use_token = api_first and not via_cookie and st_token` → token, `not ok` → фолбэк `_create_text_via_cookie` (недоделанная РК удалена token-путём); при units-фейле помечаю `res["token_units_fallback"]=True`. B2 (orchestrator): OFF-ветка флипа = прежние 3 строки байт-в-байт; ON = **строгий флип** только при подтверждении (`_units_alive_for_login is False` ИЛИ N=2 реальных 152 подряд, `units_fail_streak`), иначе сбрасываю sticky `units_seen/units_block` (одиночный 152 при живых баллах = транзиент, не уводим на куку). `token_units_fallback` тоже засчитывается в streak (иначе fallback-успех скрыл бы 152). `_channel_of`: под ON добавляю search_test/search_dynamic в units-типы (канал A растёт). Флаг: `os.getenv("DIRECT_API_FIRST")`, читается на каждый набор, в unit-env НЕ прописан → прод OFF. Верифай: py_compile Mac+LXC, import+deps-wiring на LXC (`_build_text_from_pack` в feed_deps, `_create_text_via_token` в orch-deps), 7 routing-кейсов стабами (OFF→cookie, ON+ok→token, ON+152→token+cookie+mark, ON+APIfail→token+cookie+без mark, ON+via_cookie/no-token→cookie), сервисы active. **Живьём token-путь tp2/tp4 НЕ прогонял (C2, porg-psm5h7q6)** — баллы, не деплою прогон. ⚡ инстинкт: token-путь tp2/tp4 ≠ cookie автоматически — v5 adgroups.add НЕ даёт автотаргет-профиль, всегда добивать relevanceMatch через Grid `update_unified_adgroups`.

## 2026-07-09 · G: набор-level пред-заливка картинок tp1 ДО цикла (прогрев кэша)
Задача: чтобы создание каждой РК не блокировалось на аплоаде картинок, залить ВСЕ уникальные креативы набора ОДИН раз параллельно ДО цикла. Ключевое открытие: заливка креативов через Grid делает РЕАЛЬНО только **tp1** — tp5 skip (картинки у ShoppingAd/ListingAd, `_build_tp1_adgroups` `if tp_code!="tp5"`), tp2/tp4 запрещены правилом Семёна (`image_paths=[]` в `create_set_text_builders`), tp3 без креативов. Поэтому пред-заливка = tp1-only. Механика фолбэка ВСТРОЕНА: `_cached_upload_image` держит процесс-глоб. `_GRID_IMG_HASH_CACHE` (ключ login+realpath, thread-safe) → достаточно ПРОГРЕТЬ его до цикла; per-РК `_parallel_upload_images` в цикле сам переиспользует, кэш-промах = штатная заливка (грациозно, без отдельного проброса basename→hash). Реализация: `_preupload_tp1_images(login,items,site,slepok,grid_cookie)` в `create_set_tp1_builders.py` (после `_grid_account_image_hashes`) — резолвинг НЕ переписывал, зову тот же `kp.gather(key,site,"tp1")`→cts(positive)→сегментный фильтр(`tp1_segment`,None→все,union по пунктам)→`_creative_images_for_ct(site,"tp1",ct,key)[:5]`→дедуп против `_grid_account_image_hashes`(0 баллов, account_map)→`_parallel_upload_images`. Запуск: фон-демон в `create_set_orchestrator.py` после прайминга контент-префетча, только если есть `tp1_rsy` (helper само-фильтрует). Grid `upload_image` = по куке, БАЛЛЫ не тратит (безопасно пред-грузить). Грабля: `_grid_account_image_hashes` читает хэши из СУЩЕСТВУЮЩИХ ОБЪЯВЛЕНИЙ (не из библиотеки) → свежезалитые-но-не-прикреплённые не видны там; поэтому дедуп по нему = только «созревший» аккаунт, а внутрипроцессный дедуп несёт `_GRID_IMG_HASH_CACHE`. Верифай: py_compile Mac+LXC, pyflakes (только configure-инъекции), 4 runtime-стаба (no-op фильтр/union want_all/seg-filter/account_map+cookie passthrough), сервисы active, import LXC OK; живьём (замер) НЕ мерял — только перенос момента. ⚡ инстинкт: `create_set_tp1_builders` использует configure-инъекции (`globals().update(deps)`) → pyflakes «undefined name» для kp/gf/_SLEPOK_KEY/… это НОРМА, проверять что имя есть в `_create_set_tp1_builder_deps()` (blueprint), а не что pyflakes чист.

## 2026-07-09 · C1: параллельные НЕконкурирующие каналы создания (фича-флаг, дефолт OFF)
Задача: пока РК создаётся по API (тратит агентские units) — параллельно создавать РК по куке (баллы не тратит), т.к. не конкурируют. Классификация тип→канал (проверена кодом, НЕ по названию tp): **Канал A (units)** = `tp1_rsy`,`rsya_gallery`(tp3),`search_gallery`(tp5) — идут `create_tp1/tp5/tp3_campaign` v501/token, 152→cookie фолбэк. **Канал B (без баллов)** = `search_test`(tp2),`search_dynamic`(tp4) — ВСЕГДА cookie/Grid (`create_text_via_cookie`; историческая v501-ветка мёртвая за `if True:…continue`), и tp6/tp7 master/product — UAC/cookie. Грабля: НЕ верь дизайн-премиссе «tp2/tp4=API» — они давно cookie-only. Реализация (`create_set_orchestrator.py`): прежний `for` вынесен в замыкание `_run_item(_ci,it,ch)` (break→`ch.stop=True;return`, continue→`return`); канал-локальное состояние (results/scan_i/units_*/via_cookie/tp7_mf) на `_Chan`. Флаг `DIRECT_PARALLEL_CHANNELS` (дефолт OFF): OFF = один `_Chan(results,via_cookie)` + обычный sequential for (семантика идентична, обёртки job-хелперов = сами оригиналы); ON = партиция items по индексу A/B, 2 daemon-Thread, РАЗДЕЛЬНЫЕ списки результатов (нет гонки append, скан-152 A изолирован от B), join+merge. Локи: `_guard`=RLock(ON)/nullcontext(OFF) вокруг job/prefetch/content-cache мутаций; master/product канала B бампает счётчики напрямую → добавлен `_JOB_MUT_LOCK` внутрь `blueprint._bump_job/_bump_item` (uncontended в OFF). Порядок локов _guard→_JOB_MUT_LOCK, без цикла. created/failed пересчитываются из results в финале → счётчик-гонка косметика. Грабля-раздельные-results: не блокировать общий список — дать каждому каналу свой и слить после join (проще+безопаснее). Верифай: только статика (py_compile+pyflakes-undefined+import LXC OK, сервисы active, route 302, флаг OFF); живьём НЕ прогонял (шаг C2, porg-psm5h7q6). ⚡ инстинкт: правки этого цикла гонять pyflakes на undefined (py_compile не ловит `name 'login' is not defined`).

## 2026-07-09 · LLM: разделили «зависание» и «время генерации» — стрим + idle-таймаут
Задача E. Симптом: рабочая долгая генерация 72B рвалась одним wall-clock read-timeout «как зависание» → ранний фолбэк на слепок. Корень: не-стрим POST, таймаут тикал ПОКА модель генерит. Фикс (`llm_providers.py`): `_m3_complete`/`_m3_complete_url`/`_or_complete_url` → `stream=True` + `timeout=(connect, idle)`; новый `_consume_sse_stream` аккумулирует `delta.content` и бьёт heartbeat НА КАЖДОМ чанке. Idle = тишина МЕЖДУ токенами (env `*_IDLE_TIMEOUT=45`), НЕ общий wall-clock → зависание = read-timeout/обрыв. Параметр `timeout` вызовов теперь переопределяет idle-gap (сигнатуры сохранены). Грабля: `iter_lines(decode_unicode=True)` на `text/event-stream` без charset → latin-1 → кириллица OpenRouter в мойибаке; всегда `decode_unicode=False` + сам `.decode("utf-8","replace")`. Грабля-2: DeepSeek V4 Flash — reasoning-модель, стримит `delta.reasoning` (content=''), потом `delta.content`; берём ТОЛЬКО content; малый max_tokens → reasoning съедает бюджет → пустой content (тест-артефакт, не баг). Верифай: стаб-сервер (idle сработал/живой-медленный собрался) + реальный M3/OR на LXC101.

## 2026-07-07 · Haval ct0111 видео пустое при audit still_missing:0 — ИСПРАВЛЕНО
Брендовая «Марки»-ct0111 (Haval): feeds_ct_model()["ct0111"]=None → brand_word="" → brand-fallback пропускался.
ИСПРАВЛЕНО: videos_pool_for_ct(brand_hint="") принимает подсказку от вызывающего кода;
_ct_has_pool_video резолвит бренд из _DEPS["_ag_part1_map"]() → ag_part1["ct0111"]="Haval"
→ brand_hint="Haval" → brand_word="haval" → сканирует ct0112–ct0117 → пул непустой → VIDEO_MISSING корректно эмитится.
Ключевое: kontent_pack.py БЕЗ БД — brand резолв остался в campaign_spec_audit.py (там _ag_part1_map уже в _DEPS).
Файлы: kontent_pack.py:1218(сигнатура),1253-1254(fallback); campaign_spec_audit.py:672-680(brand_hint резолв).

## 2026-07-08 · Диагностика "пропали tp5" при рестарте direct-worker в процессе джобы
Кейс: job 0aa965f0585b (porg-psm5h7q6) прерван watchdog (interrupted). Live v5 campaigns.get(Ids=[...]) → все 10 tp5 (+4 tp7) NOT FOUND = удалены; в кабинете остались лишь 14 DRAFT (tp1x10+tp2x4, State=OFF/Status=DRAFT, adgroups есть, но не финализированы/не запущены).
Диагностика live-состояния кампаний: standalone python на LXC101 через loader.load_yandex_direct()["tokens"] + POST api.direct.yandex.com/json/v5/campaigns (Bearer + Client-Login). Джобы/репары — loader.load_db("victory") (это Victory Postgres, БД direct_automation_jobs/direct_delayed_repairs/direct_deferred_creates в public). Create-set исполняет direct-worker.service (не direct.service); журнал кампаний почти не логируется в journal.
Грабли: (1) content_repair (direct_delayed_repairs.result) с CAMPAIGN_NOT_FOUND = кампания удалена ДО добивки — точная нижняя граница времени удаления. (2) UI-карточка джобы показывала "10 битых... джоба edc293817bbf в очереди" — edc293817bbf НЕ в automation_jobs (TTL-purged), это parent старого репара 17:03 того же логина → карточка показывает УСТАРЕВШУЮ заметку от прошлого набора (UI-баг). (3) fix_generic_fallback_group/WRONG_AUTOTARGET-recreate ВСЕГДА ставят deferred ДО delete — если deferred_creates пусты, значит удаление шло НЕ этим путём (либо recreate-job потерян при рестарте: sibling e17a9781011f получил recreate 2175bc749e5d, а 0aa965 — нет).
Вывод: рестарт worker в процессе создания набора → кампании удаляются, а requeue/deferred теряется. Набор нерабочий: докрутка в удалённые = CAMPAIGN_NOT_FOUND; лечится только пересозданием набора заново (снести 14 DRAFT + новый прогон).

## 2026-07-07 · Проверка porg-7bqj56f4 (job e17a9781011f) — tp5 спаслись, tp7 удалены
Тот же класс, что 0aa965/porg-psm5h7q6: worker restart в середине набора. НО исход другой.
Live v5 all-types get: 12 кампаний PRESENT (5 tp1 + 2 tp2 + 5 tp5 recreated 712304633/831,305055/467/925) все DRAFT/OFF. Старые tp5 (712301900..304196) корректно удалены recreate-джобой 2175bc749e5d (done, c5/d5/f0, 20:14→20:27) — она успела ДО рестарта. 2 tp7 Товарка (cid 712304419,712304466, ok=True в result.results[12,13], прошли live_verification) → campaigns.get Ids=[...] = {"Campaigns":[]} = УДАЛЕНЫ. Активного пересоздания tp7 НЕТ: last delayed_repair 56743ac1f87a (parent e17a, content_repair, partial/ok=False, attempts1, CAMPAIGN_NOT_FOUND на СТАРЫХ tp5 — таргетил pre-recreate id), deferred_creates только старый df7f70e (07-06 done).
journalctl direct-worker: Stopping 20:38:56 → restarted 20:47:57 (деплой-рестарт), parent watchdog interrupted 21:01. Рестарт попал между recreate-done(20:27) и content_repair-update(20:41) → tp7 не переехали.
Вывод: разница с прошлым кейсом — recreate tp5 успел завершиться до рестарта, поэтому tp5 живы; tp7 (не имеют recreate-пути, удаляются как битая товарка без фида) потеряны без requeue. Итог набора: 12/14 живых, 2 tp7 Товарка отсутствуют, восстановления нет.

## 2026-07-08 · porg-ozge4ntu "нет tp5" — НЕ баг, слепок pavlov без tp5
Job f37727563faa (agent=pavlov, Мультибренд): tp5 отсутствуют, но это НОРМА, не WRONG_AUTOTARGET-удаление.
body.items = tp1:5,tp2:3,tp6:10,tp7:3 (нет tp5 в посланном плане); result.per_tp то же; repair_gate ВСЕ нули (recreate_delete=0); deferred_creates для джобы нет; delayed_repairs только content_repair skipped 0 действий. Live v5: ровно 5 tp1+3 tp2 (13 UAC tp6/tp7 в Grid, v5 их не отдаёт) — 21/21 DRAFT/OFF.
Корень: create_set_plan.py:487-492 — tp5 эмитится только если слепок его содержит (_tp_plan_names/_slepok_profile_excludes_tp). Слепок pavlov структурно БЕЗ tp5 (вместо него 10 tp6 UAC). Слепки scherbakova/terehov в тот же день ДАЛИ tp5 (5/5/3) на тех же Мультибренд-аккаунтах — разница именно в agent=слепок, не в коде воркера.
Грабля: v5 campaigns.get даже с явными Types отдаёт только TEXT_CAMPAIGN (8 шт); Мастер кампаний/Товарка (UAC) в v5 НЕ видны — считать их через Grid live_verification (grid_rows/uac_detail_rows), иначе кажется что набора нет.
Вывод: если у аккаунта нет tp5 — сначала сверить agent(slepok) в body: pavlov=без tp5 by design. Чинить нечем, это выбор слепка, а не потерянный набор.

## 2026-07-08 · Проактивная проверка tp5-потерь (system-bug worker-старый-код 07-07) — lzjk6p5m
Проверил все set-джобы после 18:42 UTC (=21:29 MSK, порог рестарта). automation_jobs TTL-purged → осталось мало; полный список за сутки виден.
Затронутые новым багом (live_verification WRONG_AUTOTARGET на tp5 → delete): psm5h7q6, ozge4ntu (другие агенты), 7bqj56f4 (память), и НОВЫЙ — porg-lzjk6p5m job 661783bc3bae (interrupted, agency victorylotsofads1).
lzjk6p5m: план 27 tp5 (cid 712302149..712309653), live v5 = 22 present (все OFF/DRAFT), 5 удалены: 712302549,712304187,712305193,712309026,712309274. ТОЧНО = 5 записей ok=False в result (WRONG_AUTOTARGET). 22 ok=True выжили.
ГРАБЛЯ recreate: resume-джоба c5d77335a5ed (3 сегментных tp5 Марки/Модели/Общее, _resume_of=661783bc3bae) НЕ пересоздала — все 3 item ok=True, note "уже создана в Директе — пропущена при докрутке". Дедуп ПО ИМЕНИ сегмента увидел выжившие одноимённые tp5 (22 шт содержат Марки/Модели/Общее) → счёл «уже есть» → 5 удалённых регион-вариантов НЕ восстановлены. deferred cb22347b2023 "создано 0, не создано 0".
porg-asfbs7qe (3168a0ad125f, done 21/21) — 0 tp5 в наборе, НЕ затронут; repair 59846a6b9456 done/ok.
Токен live: yd=loader.load_yandex_direct(); token=yd["tokens"]["victorylotsofads1"]["oauth_token"] (НЕ .access_token — dict-ключ oauth_token!). Client-Login=porg-*. v5 campaigns.get by Ids даёт точную картину present/deleted.
Вывод: lzjk6p5m требует восстановления 5 tp5; докрутка сама не починит (name-dedup-скип).

## 2026-07-08 · Восстановление 5 удалённых tp5 lzjk6p5m — механика fan-out + хирургия (продолжение выше)
Симптом: 5 tp5 снесены багом, name-dedup-скип докрутки не восстанавливает.
Корень механики: tp5(=type search_gallery) в оркестраторе — ОДИН item на сегмент (Марки/Модели/Общее),
но `_create_tp5_campaign` фан-аутит по ВСЕМ URL-фидам аккаунта (тут 9) × сегмент = 27 кампаний (cpc-only при no_cpa).
Имя = base(«tp5_cpc_site — … - {seg} - КС - Россия») + « — {feed_url_без_https}». Дедуп в оркестраторе идёт по
BASE-имени сегмента (already_in_direct) → раз выжила хоть одна фид-кампания сегмента, ВЕСЬ item скипается →
недостающие фид-варианты не досоздаются. Внутри `_create_tp5_campaign` пофидового дедупа НЕТ → повтор через
оркестратор/force_names создал бы дубли 22 выживших.
Фикс-хирургия (без дублей): вычислить недостающие (segment,feed) сравнив live-имена с полным набором
9фидов×3сегмента (той же формулой имени), затем ПРЯМОЙ вызов
`fb._create_tp5_single(data, tok, login, nm, "tcpa", goal, cpa, budget, counter, region_ids, href, feed_id,
feed_name, agent, site_type, r_code, corr, ret_map, feed_models=…, titles=tpl_titles, city, segment, …)`
ровно для недостающих. data=`fb._tp5_account_data(tok,login,agent,site_type,agency)`; fb=`bp._create_set_feed_builder_module()`.
Контекст: ctx=`bp._account_ctx(login)` (domain/geoid/city/agency), href="https://"+domain, region_ids=[geoid=225],
r_code=`bp._resolve_region(city)[0]`, corr=`bp._load_corrections(city)`, ret_map=`bp._account_retargeting(tok,login)`,
feed_models из `bp._account_model_feeds(login,agency)` по feed_id. launch=False (инвариант — черновики).
Результат: 2/5 создано OK (drafts), 3/5 БЛОК внешним — фид `dostup-k-rasprodazhe-01-a.xml` Status=ERROR/items=0
(feeds.get FieldNames=[Id,Name,Status,NumberOfItems]; UpdateStatus — НЕВАЛИДНОЕ поле, будет 8000). При ERROR-фиде
Grid addShoppingAds → FEED_STATUS_WRONG → `_create_tp5_single` удаляет частичную кампанию (partial_deleted, defer)
→ сирот НЕТ. Верификация: v5 campaigns.get, tp5 22→24, имена уникальны (Counter dup=0).
Грабля feeds.get: SelectionCriteria без Ids = 8000 «Отсутствует Ids» (нужны явные Ids).
Вывод: досоздать 3 когда фид 3517781 станет DONE — тем же per-feed `_create_tp5_single`, НЕ оркестратором.

## 2026-07-08 · offerRetargeting НЕ входит в retargetings_present (tp5 skip-gate корректен)
grid_finalize.py:2055-2065: retargetings_present = gid in rt_groups, rt_groups строится из GdRetargeting (audience-условия). offerRetargeting (товарная галерея) хранится отдельно как offer_retargeting и в rt_groups НЕ попадает. Вывод: skip-гейт step 4.5 (create_set_feed_builders.py:618) и repair_executor.py:627 КОРРЕКТНЫ — tp5 товарные группы с offerRetargeting не фильтруются. Менять гейт НЕ нужно.

## 2026-07-08 · Split web/worker УЖЕ развёрнут на LXC101 (Q2-премисса устарела)
direct.service = DIRECT_ROLE=web (только UI + POST джоб в БД, воркеров/демонов/поллера НЕТ — blueprint.py:2499 web-роль возвращается сразу). direct-worker.service = DIRECT_ROLE=worker (active/running, ВЛАДЕЕТ созданием РК: пул воркеров+демоны+БД-поллер web-posted джоб). direct-content.service + direct-copy.service тоже отдельные процессы. Все 4 enabled+active. Контракт очереди — public.direct_automation_jobs (web клеймит queued→claimed FOR UPDATE SKIP LOCKED, blueprint.py:2547).
Значит: рестарт web-UI НЕ трогает in-flight наборы (это и был смысл split). ОСТАВШАЯСЯ дыра (ERRORS_JOURNAL K): рестарт САМОГО direct-worker в середине набора теряет позиции между delete и create. worker_main drain=540s только перестаёт брать НОВЫЕ джобы; текущая running-джоба >540s → SIGKILL. Митигация: _jobs_db_recover→_bg_sweep→_requeue_missing_positions_once (blueprint.py:757-797).
Грабля: worker_main.py и deploy/direct-worker.service существуют, но в git/файлах некоторые строки санитайзятся ("DIRECT_ROLE"→"n" в grep артефактах) — смотреть реальный systemctl show, не только grep.
Финализация НЕ идёт по cookie_only=True: create-set вызывает get_grid_client(login, cookie=grid_cookie) БЕЗ cookie_only (grid_finalize.py:125) → token→Grid replication sleeps (set_default_text 1.2с/фид :1166, add_shopping_ads lag :1285) АКТИВНЫ и обоснованы (кампании создаёт токен v5, финализирует Grid-кука).

## 2026-07-08 · Профилирование "долго создаётся набор" (job 1700f39654f1, porg-psm5h7q6, 14 РК, 2120с)
Метод профиля: journalctl -u direct-worker.service --since/--until (сервер TZ=+05 Екб; UTC+5). Внутренности create-set в journal почти НЕ логируются — профиль строится по РЕДКИМ маркерам ([tp1] feed=create-событие, [img-cache] MISS->upload, [llm-fallback], [keywords_repair]=добивка) + тёмным паузам между ними + фикс-costs из кода.
Разбивка (best-effort): serial per-campaign loop ~85% (14:29→14:59, ~2.1мин/РК, worst 6.8мин); из них LLM-стойла ~20-28% (M3 127.0.0.1:8086 DOWN весь прогон → read timeout=90×3 + OpenRouter Max retries 14:41/14:43 = дыра 410с); img upload 453 cold но parallel(8) быстро (~2мин, НЕ боттлнек); post-verify+repair-gate 14:59→15:01 ~120с; delayed content_repair (keywords→200 усечение) tail +180с wait +~200с.
ТОП причина: последовательный цикл создания (blueprint.py:268 _CREATE_MAX_PER_AGENCY=1 + сам for-loop create_set_orchestrator.py:385) + LLM-инфра (M3 down) + фикс-sleeps (grid_finalize.py:1166 sleep1.2/фид).
Грабля: content prefetch workers=2 (create_set_orchestrator.py:344), overlap с созданием есть но мелкий → LLM-латентность плохо прячется когда M3 down.
Добивка keywords_repair (repair_executor.py:23 _KW_MAX_PER_GROUP=200) фиксит группы >200 ключей ПОСТ-фактум, хотя create-пути уже капят до 200 (create_set_text_builders.py:92, tp1_builders:252, grid_create.py:456) — значит ЕСТЬ путь без капа (tp7/товарка cid 712408001) → NO_KEYWORDS_LIVE → лишний post-round. Кандидат на устранение добивки.

## 2026-07-09 · Слепок dmp: 34 уникальных группы 1:1 к реальному аккаунту dmp.one
Задача: предыдущий инстанс смержил 34 группы в 7 "ct"-блоков — НЕПРАВИЛЬНО. Исправление: каждая реальная group object = отдельная ct-группа, без слияния. Архитектура: 1 tp-код = 1 кампания (нет механизма нескольких кампаний одного tp в одном site_type). Следствие: 4 реальные кампании (Идентификация 14гр, Маркет.инструменты 2гр, Околотематика 2гр, Расширенное СЯ 16гр) лягут в ОДНУ tp2-кампанию с 34 группами — это архитектурное ограничение. ct-коды ct0001–ct0034 изолированы пространством namespace "dmp" (путь `/opt/neuro_content_local/kontent_oktyabr/dmp/tp2/ctXXXX/`) — не конфликтуют с авто-слепками где те же коды = бренды. JSON файл-источник: `/scratchpad/dmp_extracted_full.json`, источник правды — 97242721 (не 97242648-дубль). Все minus_words пустые — slepok_minus_keywords НЕ обновляли. Контент-пак: 34 директории по 1 файлу keywords/dmp.txt, 1175 ключей суммарно. ⚡ Инстинкт: для нестандартных слепков (не автосалоны) ct-коды могут быть локальными именами — главное чтобы `name`/`t` совпадали с реальными названиями групп из аккаунта 1:1.

## 2026-07-11 · salamahin/karavaev tp5 = РЕАЛЬНО AON (не aoff), tp1 salamahin — 311 файлов скопировано
Задача: добрать ключи для salamahin/karavaev в пустых КС-группах. Находка: tp5 («Поиск + Динамика + Товарная галерея») у обоих директологов реально имеет ТОЛЬКО 1 generic aon-группу (ct0034_aon у Саламахина, ct0014_aon у Карав.) с generic-запросами «купить авто», «приобрести машину» — НЕ brand-specific aoff-групп. Структурные 106 aoff-items в tp5 = synthetic/template, НЕ из реальных аккаунтов. РЕКОМЕНДАЦИЯ: убрать/заменить aoff→aon в tp5 salamahin (4 сегмента × 106 = 424 items) и karavaev (106 items). Keyword-файлы для tp5 brand ct НЕ нужны. Для tp1 salamahin — все 4 сегмента реально КС. Скопировано из Мультибренд/tp1: Монобренд(87), С пробегом(201), Квиз(23) = 311 файлов. ct0300 (Tenet) нет в corpus Саламахина ни в одном аккаунте — рекомендация убрать из структуры. Оба носителя: LXC101 + M3. ⚡ Инстинкт: tp5 = ОДНА generic aon-кампания на аккаунт (не отдельная кампания на каждый бренд) — keyword-файлы для brand-ct в tp5 НЕ нужны, это ложный aoff в структуре. rsync для путей с пробелами (С пробегом) — не работает через bash heredoc в pct exec; нужен Python subprocess tar+pipe.

## 2026-07-09 · dmp tp2: splits добавлены в JSON, НО splits НЕ создают 4 кампании для tp2
Задача: добавить splits в dmp/dmp/tp2 чтобы получить 4 кампании (14/2/2/16 групп). JSON изменён корректно: `slepki_structure.json` строка ~76483, `"groups"` заменён на `"splits"` с 4 блоками (sq: identification/marketing_tools/offtopic/extended_sya). `_tp_plan_names` (create_set_plan.py:143) ЧИТАЕТ splits правильно — возвращает все 34 items флатом, проходя все 4 блока. НО план-билдер tp2 (search_test ветка, строки 461-484) НЕ использует split-границы для создания кампаний: группирует items по `_ct_segment(gc)` → сегменты Марки/Модели/Общее → 1 кампания на сегмент. Dry-run подтвердил: 34 items возвращаются корректно, но «4 кампании per split» — НЕ работает для tp2. Это ограничение только tp2/tp1/tp4/tp5: все используют `_tp_plan_names` + сегментную логику. Для tp6/tp7: `_emit_struct` + `_slepok_struct_groups` — там splits/группы ДЕЙСТВИТЕЛЬНО дают отдельные кампании. ⚡ Инстинкт: splits в slepki_structure.json = отдельные кампании ТОЛЬКО для tp6/tp7 (_emit_struct). Для tp1/tp2/tp4/tp5 splits = структурная документация, на количество кампаний не влияет (сегментная логика). Чтобы дать dmp 4 кампании нужен новый plan builder или tp6/tp7-стиль обработки.

## 2026-07-11 · Батч porg-vfdnaolu: «ушло в докрутку / 0 создано» = ПУСТОЙ М3-пак, НЕ units/152
Задача: тест-прогон создания РК на porg-vfdnaolu по 10 авто-слепкам (после пилота pavlov). Создано 19 черновиков (pavlov9/scherbakova6/kryuchkova4/gordeeva0), потом остановлено координатором на диагностике. СИМПТОМ: gordeeva ушла в докрутку, 0 создано — координатор заподозрил, что tp6-грайндинг съел операторские баллы (152). КОРЕНЬ (опровергнута гипотеза units 3× ): (1) `direct_deferred_creates` note=«докручено по куке: создано 0» — докрутка units-free; (2) cookie-путь баллы НЕ трогает (blueprint.py:2257); (3) прямой `bp._pack_for_item(slepok,site_type,'tp1',real_gc)` → gordeeva/С пробегом from=**fallback kw=0**, scherbakova/С пробегом from=**pack kw=424**. Т.е. per-slepok М3-пак не наполнен для выбранного site_type. ФИКС: probe `_pack_for_item` ДО прогона (реальные gc из slepki_structure.json, не угадывать `ct0000_aoff`). Матрица: gordeeva/karavaev пусты на С пробегом но полны на Мультибренд; **kuderko пуст в ОБОИХ** (нужен реальный fill). ВТОРАЯ грабля: tp6/tp7 (master/product=UNIFIED/SMART) на аккаунте без счётчика Метрики → `goals[0].goalId MUST_BE_VALID_ID value "0"` HTTP400, no_cpa НЕ гасит (это тип кампании, не cpa-стратегия), 51 мастер × ~10мин UAC-ретраи заморозили воркер. ⚡ Инстинкт: перед батч-прогоном создания по слепку ВСЕГДА probe наполненности пака (`_pack_for_item` from==pack? kw>0?) и фильтруй план по pay!='cpa' И tp∉{tp6,tp7} на аккаунтах без Метрики. «Docrutka по куке = 0» ≠ баллы, ищи пустой пак.

## 2026-07-11 · Харвест пака: kuderko/salamahin/terehov/scherbakova — диагностика aon vs КС
Задача: заполнить пустые pack-файлы, объяснить «212 пустых ct» scherbakova. Результат по 4 слепкам:
(1) kuderko = ВСЕ aon в структуре; probe показывал pack=0 т.к. первые 12 gc = brand-ct (ct0181/ct0164/…), ct0000 идёт на позиции 13+. `read_keywords` НЕ ИМЕЕТ ct0000-фолбэка — каждый ct нужен явно. Фикс: записал 4878 kw (реальных «бу авто» из 4 доступных аккаунтов из 54) во все 62 brand-ct папки С пробегом/tp1 и tp2. Probe: pack=12 fb=0 kw=58536. Мультибренд kuderko — api_4001 на всех 4 аккаунтах (чужое агентство), пак не заполнен. ⚡ Инстинкт: bc probe checks first 12 gc items — если первые 12 = brand-ct а ct0000 дальше, нужны файлы для КАЖДОГО brand-ct в первых 12 позициях, не только ct0000.
(2) salamahin = подтверждён aon из реальных аккаунтов: 19 аккаунтов accessible, 5 уникальных фраз включая «---autotargeting» маркер Яндекса — это документальное подтверждение aon. Пак Мультибренд уже богат (58867 kw в ct0000). Структурных изменений не нужно.
(3) terehov = КС в реальных аккаунтах (3864 ключа из 4 аккаунтов), структура aon. Записаны terehov.txt в ct0000/ct0009/ct0010 для Мультибренд/Монобренд/С пробегом tp2 и tp4. «---autotargeting» маркеры удалены постфактум. Probe: Мультибренд/tp2 pack=11 fb=1. Caveat: структура aon при реальных КС-аккаунтах — возможен переход aon→aoff если Терехов захочет КС-режим.
(4) scherbakova «212 пустых ct в tp2» = ЛОЖНАЯ ТРЕВОГА. 318 директорий в паке принадлежат ВСЕМ слепкам (ct0000-ct0250). Структура scherbakova = только 103 ct кода — все 103 покрыты файлами scherbakova.txt. «212 без scherbakova.txt» = директории других слепков, не её. ⚡ Инстинкт: «пустые ct в паке» считать только относительно СТРУКТУРЫ конкретного слепка, не относительно всех папок в директории.
Граблии: «---autotargeting» маркер загрязняет keyword-файлы — фильтровать строки начинающиеся с «---» при записи. Файлы: `scratchpad/pack_autotarget_patch.json`.

## 2026-07-12 · Харвест zubakin/gordeeva Монобренд — «пак пуст» при прогоне 11 слепков

Задача: 16/18 позиций zubakin и 4/6 gordeeva упали с «пак пуст»/«нет ct-папок» при тесте на porg-asfbs7qe. КОРЕНЬ: gather("zubakin","Монобренд","tp1") находил ТОЛЬКО ct0000 (zubakin.txt там был — 198kw из Chery), но brand-специфичные cts (ct0029/ct0044/ct0097/ct0181) не имели zubakin.txt → сегментный фильтр «Марки»/«Модели» давал 0 групп → «нет ct-папок». Gordeeva: tp2 полностью пуста (gordeeva.txt только в tp1/tp5 частично). ФИКС: харвест из 15 аккаунтов (6 zubakin + 9 gordeeva, разные агентства victoryagency14/victorylotsofads1/victoryagency-direct1618440). Результат: 62k операций записи, zubakin 33 ct × tp1/tp2/tp5, gordeeva 33 ct × tp1/tp2/tp5. Probe подтверждает: gather() tp1=33 cts, tp2=31 cts; Марки=4(zubakin)/5(gordeeva), Модели=27/19. ДЫРЫ: ct0026-ct0028/ct0306 Belgee — porg-o5x73pkx имеет 0 кампаний (не заполнен), нет источника для Belgee kw. NO_BRAND_SEGMENTS_AVAILABLE (tp5) — НЕ реальный Yandex 152: `create_set_gallery.py:75, if via_cookie or not st_token:` + tp5_segment → generic-заглушка. Retry с `_resume_via_token=True` уже запланирован автоматически. ⚡ Инстинкт: перед прогоном нового слепка/типа сайта — проверять gather(slepok, segment, tp) для КАЖДОГО tp, что pack не пустой И есть нужные сегменты Марки/Модели через `_ct_segment`. ct0000 один = только «Общее», «Марки»/«Модели» не будут созданы.

## 2026-07-11 · dmp (B2B лидоген) шаг5 СТОП: env-маунт пака + гейт шаблонов + генерация даёт АВТО не B2B
Задача: прогон dmp на porg-vfdnaolu. СТОП (create не прошёл). ТРИ находки: (1) ВОРКЕР читает пак из `NEURO_PACK_MOUNT=/opt/neuro_content_local` (env юнита direct-create-worker), НЕ дефолт kontent_pack.py:31 `/opt/neuro_kontent`. Мой standalone probe без env давал ложный kw=0 — ВСЕГДА экспортируй `NEURO_PACK_MOUNT=/opt/neuro_content_local` в probe/скриптах. С верным env dmp/tp2 ct0800+ = pack15 kw557, ключи B2B («источник лид»,«поиск лидов»,«идентификация пользователи»). (2) Гейт `validate_create_set_content` (create_set_account.py:59): create падает `нет шаблонных текстов для типа сайта «dmp»` — `_templates_for('dmp')` (create_set_context.py:48, читает `public.direct_ad_templates WHERE site_type=%s`) = 0 строк (у 5 авто-типов по 5-8 title/3-4 text/8 sitelink). stream_content генерит тексты позже, но гейт срабатывает ДО. (3) КЛЮЧЕВОЕ: даже с исправным паком генерация `_cached_campaign_content(dmp,site_type=dmp)` даёт АВТО-контент, не B2B — LLM подмешивает авто-скелет («Автокредит от 9000₽/мес»,«Трейд-ин»,«КАСКО»,«Авто с пробегом в кредит»), тексты/сайтлинки ЦЕЛИКОМ авто; fast_mode → OpenRouter пустой → фолбэк `_rsya_titles`=авто. Пайплайн генерации захардкожен под автосалоны, B2B-режима для site_type=dmp нет. ⚡ Инстинкт: dmp/B2B слепок ≠ только пак — нужны ещё (а) B2B-строки в direct_ad_templates для site_type=dmp, (б) B2B-промпт ai_agents + не-авто фолбэк. Пак сам по себе НЕ даёт B2B-объявления.
## 2026-07-12 · Аудит profile-vs-structure tp (karavaev/Мультибренд tp6/tp7) — verdict определяется КОНФИГОМ, не live
Задача (read-only): structure(slepki_structure.json) объявляет karavaev/Мультибренд tp1-tp7 (tp6='Мастер кампаний'/UAC, tp7='Товарка'/Shopping), profile(targeting_profile.json) разрешает tp1-tp5. Проверить live-аккаунты. НАХОДКА: `direct_automation_jobs` — очень мелкая история (13 джоб, ~сутки 07-11→07-12, ВСЕ на тест-логине `porg-asfbs7qe`/victoryagency14). Джоб пары karavaev+Мультибренд = 0 (karavaev = 2 джобы, обе Монобренд; единственная Мультибренд-джоба = scherbakova). ⚡ Инстинкт: НЕ трать время на поиск живых аккаунтов исторической пары в `direct_automation_jobs` — таблица держит десяток свежих джоб на порождённых porg-* тест-логинах, боевых per-слепок аккаунтов там нет. Verdict profile_hides_it определяется КОНФИГОМ+КОДОМ, live не требуется: `blueprint_targeting.py:218 _slepok_profile_excludes_tp` — если у слепка ЕСТЬ профиль для site_type, а tp в нём НЕТ → build блокируется (профиль АВТОРИТЕТЕН), а `:236 _slepki_structure_for_ui` прячет такой tp из чекбоксов. structure держит tp6/tp7 как ДОНОРСКИЕ/легаси, profile режет. deeper_generation_gap невозможен когда profile НЕ содержит tp (требует обоих). Live-подтверждение (proxy porg-asfbs7qe): v5 campaigns get → 14 кампаний, ВСЕ Type=TEXT_CAMPAIGN (tp1/2/4/5), 0 UNIFIED_CAMPAIGN(UAC/tp6), 0 SMART_CAMPAIGN(Товарка/tp7). Грабля: Grid typename `GdUnifiedCampaign` = generic-контейнер обычных текстовых, НЕ UAC — сверяй по v5 Type. Live-скрипт: PYTHONPATH=/opt/scripts/home/seoadvanced /root/venv/bin/python3, `import direct.yandex_gateway`, token_for_login+v5_get.
