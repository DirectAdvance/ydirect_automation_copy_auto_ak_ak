# Кодер — унифицированное имя кампании/группы

> Кодер — это унифицированное имя кампании/группы Я.Директа, собранное из справочника
> **`public.local_gsheet_naming`** (Victory; колонки `type` / `code` / `name`). Каждый сегмент
> кодера — это `code` из своего раздела (`type`) справочника. Источник — Google-таблица
> нейминга (синк в `gsheet_naming` → `local_gsheet_naming`).

## Справочник `local_gsheet_naming` (8 разделов)

| `type` | Сегмент | Что кодирует | Примеры (`code` → `name`) |
|--------|---------|--------------|---------------------------|
| `tp`       | tp-сегмент | тип кампании | `tp1`=РСЯ, `tp2`=Поиск, `tp6`=Мастер кампаний, `tp7`=Товарка/фид, `tp8`=Telegram, `tp11`=Connected TV |
| `ag_part1` | **бренд/модель** (4-значный `ct`) | 🎯 марка/модель ИЛИ тип кластера | `ct0000`=полное отсутствие ключей (общая/без бренда), `ct0001`=кластер не определён, `ct0002`=look-like, `ct0003`=ретаргетинг+lal, `ct0019`=BAIC, `ct0021`=BAIC U5 Plus, `ct0181`=Lada, `ct0183`=Lada Granta |
| `ag_part2` | аудитории/автотаргетинг вкл/выкл | `aon`=включён, `aoff`=выключен · ⚠️ **tp6/tp7 — всегда `aon`** (`aoff` в tp6/tp7 НЕ используется) |
| `ag_part3` | интерес | `n000`=без интереса, `n025`=Lada, … (69 значений) |
| `ag_part4` | **регион** | `r0000`=плейсхолдер (на боевом → реальный код), `r0001`=Москва, … (133 значения) |
| `ag_part5` | формат (3-значный `ct`) | `ct001`=ТГО, `ct002`=Страницы каталога, `ct003`=Товарное/Фид, … (20 значений) |
| `ag_part6` | возраст | `ag001`=Все, `ag003`=18-24, `ag011`=24-55+, … (11 значений) |
| `ag_part7` | пол | `g00`=Все, `g01`=Мужчины, `g02`=Женщины |

Плюс два сегмента БЕЗ раздела в справочнике (фиксированные):
- **модель оплаты** — `cpc` (за клик) / `cpa` (за конверсию). На «структуре слепков» всегда `cpc`;
  в наборе кодер разворачивается в **обе** оплаты (cpc + cpa).
- **поверхность** — `site` (посадка = домен) / `kviz` (посадка = домен/quiz).

## 🅱️ Не-авто слепки (dmp / B2B-лидоген) — leadgen-кодер (2026-07-12)

Слепок с `"auto": false` в структуре слепка (`direct/slepki/<key>.json`, сейчас — `dmp`). У него НЕТ Марок/Моделей;
ct — это **темы кабинета** (лиды/контакты/идентификация), имена берутся из `public.leadgen_ct_naming`,
а НЕ из `local_gsheet_naming`.

- **ct-пространство dmp** (выделенное, вне авто ct0001–ct0319):
  - `ct0800–ct0833` — 34 tp2-темы (Поиск), 1:1 с выгрузкой кабинета: ct0800=«Идентификация»,
    ct0801=«СОЦ сети», ct0806=«Определение», … ct0833=«Маркетинговые СМИ». Каждая → отдельная РК.
  - `ct0000` — общий (МК Авто / МК Ключи).
  - `ct0834` — «МК Конкуренты» (выделенный leadgen-номер).
- ⚠️ **ct0032 — НЕ использовать для dmp:** совпадает с авто (`gsheet_naming`: ct0032=Changan CS55).
  Использовать ct08xx. (По-прежнему запрещено.)
- **ct0084 — РАЗРЕШЕНО для dmp (исключение, решение Семёна 2026-07-18):** ct0084 = «FAW» в авто-справочнике,
  но = «Конкуренты» в `public.leadgen_ct_naming`. Исключение работает за счёт инверсии приоритета:
  `create_set_text_builders.py:435`, `create_set_tp1_builders.py:860,1749` — для `auto:false`-слепков
  `_struct_names` (из структуры) идёт ПЕРЕД `ct_name` (справочник марок):
  `raw_name = _struct_names.get(ct) or ct_name.get(ct) or ct`. Условие инверсии — `if _struct_names:`
  (только non-auto слепки возвращают непустой `_struct_ct_names`). Для авто-слепков ct0084 по-прежнему
  резолвится как FAW (ветка `else` в тех же строках). Группа dmp/ct0084 не тянет FAW-бренд,
  не срубается mark-фильтром (brand="" для «Конкуренты»).
- **Имя группы не-авто** (`_tp1_pack_groups` / `_build_tp1_from_pack`, фикс 2026-07-12):
  `leadgen_ct_naming` (описание кодера) → тема из структуры `t` (выгрузка) → ct. Авто-фид
  `feeds_ct_model` (давал «— Авто» всем) для не-авто НЕ используется (`_struct_ct_names`).
- **Источник ключей** — пак наполняется из выгрузок кабинета (`kontent_pack.py:gather` читает
  ЛОКАЛЬНОЕ зеркало 101, не ssh на M3). `leadgen_ct_naming` (БД Victory) держать 1:1 со структурой `t`.

### 🅱️➕ Боевые правила dmp (переносимо, подтверждено масштабом 56 РК 2026-07-14)

Всё ниже — следствие «dmp = ПРОЧАЯ сфера (B2B-лидоген), НЕ авто». Авто-логика (марка/модель,
«Авто»-фолбэк, марочный фильтр ключей) для `"auto": false` НЕ применяется (`_NON_AUTO_SITE_TYPES`).

- **Нумерация ct = leadgen-неймспейс ct0800–ct0833** (+799 от старой авто-нумерации ct0001–ct0034).
  Пак-папки dmp на M3/зеркале ДОЛЖНЫ быть в этой нумерации, иначе пересечение с `only_cts` плана =
  пусто → 0 групп → defer (десинк ct). `+799` — семантически подтверждённый сдвиг 7 групп.
- **Автотаргет tp2 dmp = УЗКИЙ** (только «Целевые» + «без бренда» = `relevanceMatchCategories=[EXACT_V2_MARK]`
  + `autotargetingBrandSettings=[WITHOUT_BRAND]`). Ставится **атомарно** при Grid `AddUnifiedAdGroups`
  (profile `search_tp2`), НЕ пост-патчем `UpdateUnifiedAdGroups` (хрупок: лаг репликации + round-trip
  дублей рубит батч). Ключи добиваются пост-create через keywords_repair как safety-net.
- **Транспорт ключей = единый с созданием групп.** Если группы льются Grid'ом (`AddUnifiedAdGroups`),
  ключи ТОЖЕ Grid'ом (`add_keywords`), НЕ v5 `keywords.add`: v5 на свежих Grid-группах = лаг репликации
  Grid→v5 → ключи-фантомы (live=0). (Эталон — cookie-путь `create_full`.)
- **Промо dmp = text-only** (`amount/unit/prefix=null`, `type=PROFIT`). Промо надо **СОЗДАТЬ в кабинете
  заранее** отдельной сущностью (`PromoClient.add`), тогда кампании к ней привяжутся. Grid НЕ требует
  amount для text-only (гипотеза «PROFIT не text-only» снята).
- **Уточнения (callouts) — только tp2** (авто-подтяжка из `public.direct_slepok_callouts` в headless при
  пустом body). МК tp6/tp7 (UAC/МК) уточнения НЕ поддерживают.
- **Нейминг групп** (повтор для наглядности): по кодеру (`leadgen_ct_naming`) → иначе тема из слепка `t`,
  БЕЗ gc-хвоста и «— Авто».

## ⚠️ Главное: где «живёт» кодер — кампания vs группа

Структура имени РАЗНАЯ для UAC-типов (tp6/tp7) и для ЕПК-типов (tp1–tp5):

### tp6 / tp7 (Мастер кампаний / Товарка) — кодер ЦЕЛИКОМ на уровне КАМПАНИИ
Структура кодера (все сегменты, для понимания), применяется как идентификатор позиции:
```
tp6 _ cpc _ site _ ct0181 _ aon _ n025 _ r0000 _ ct001 _ ag011 _ g00
 │     │     │      │        │     │      │       │       │       └ ag_part7  пол (Все)
 │     │     │      │        │     │      │       │       └ ag_part6  возраст (24-55+)
 │     │     │      │        │     │      │       └ ag_part5  формат (МК=ct001; ТК=ct010)
 │     │     │      │        │     │      └ ag_part4  регион (плейсхолдер)
 │     │     │      │        │     └ ag_part3  интерес
 │     │     │      │        └ ag_part2  аудитории (ВСЕГДА aon; aoff в tp6/tp7 НЕ используется)
 │     │     │      └ ag_part1  БРЕНД/МОДЕЛЬ (4-значный ct)
 │     │     └ поверхность (site)
 │     └ оплата (cpc/cpa) — ⚠️ в ИМЕНИ КАМПАНИИ tp6/tp7 НЕ пишем
 └ tp  тип кампании (Мастер кампаний)
```

**Позиция-кодер** (поле `gc` в структуре слепка (`direct/slepki/<key>.json`); без tp/pay/surface-префикса):
- tp6 (МК): `{сегмент_ct}_aon_n000_r0000_ct001_ag011_g00`  ← `ct001`=МК
- tp7 (ТК): `{сегмент_ct}_aon_n000_r0000_ct010_ag001_g00`  ← `ct010`=ТК
(`create_set_plan.py:74-84`; aon — всегда, единый порядок по всем слепкам)

### tp1 – tp5 (ЕПК) — кодер РАЗДЕЛЁН: кампания + группа
- **Кодер КАМПАНИИ** = `tp{N}_cpc_site` (или `_kviz`). Пример: `tp5_cpc_site`.
- **Кодер ГРУППЫ** = остаток: `ct0181_aon_n025_r0000_ct001_ag011_g00`.

То есть бренд/модель/интерес/регион/формат/возраст/пол у tp1–tp5 задаются на уровне **группы**,
а имя кампании — короткое (`tp5_cpc_site`).

## 🎯 Бренд/модель = первый `ct` с 4 цифрами (`ag_part1`)

В кодере ДВА сегмента с префиксом `ct`:
- `ag_part1` — **4 цифры** (`ct0181`, `ct0000`, `ct0019`) → **бренд/модель** (источник правды);
- `ag_part5` — **3 цифры** (`ct001`, `ct009`) → формат.

**Правило определения марки/модели: берём ПЕРВЫЙ `ct` с 4 цифрами** (это `ag_part1`).
- `ct0181` → Lada · `ct0183` → Lada Granta · `ct0019` → BAIC · `ct0021` → BAIC U5 Plus.
- **`ct0000` = «полное отсутствие ключей»** = общая/без бренда → даём общий мультибрендовый текст
  (а не текст под конкретную марку).

## Подстановка региона (`r0000` → реальный)

`r0000` в структуре слепков — плейсхолдер. На боевом подставляется реальный код региона по городу
аккаунта: город → область → `ag_part4`.code (см. `blueprint.py::_resolve_region`,
таблицы `local_gsheet_yandex_direct_id_location` + `local_gsheet_naming` type=`ag_part4`).
Пример: Кемерово → «Кемеровская область (Кузбасс)» → **`r0100`**.

## ✅ КАНОН НЕЙМИНГА ПРИ СОЗДАНИИ (согласовано)

> **Имя кампании/группы ОБЯЗАНО нести кодер** — инвариант сборки (наряду с `CAMPAIGN_INVARIANTS.md`).
> Не «Марка/Марки/Модели» как имя и не произвольный текст. **Регион в ИМЕНИ не пишем** — он зашит
> в `r`-коде кодера.

### ⚙️ Правила выбора ag_part5 (формат объявления) — КЛЮЧЕВОЕ

| Тип объявлений в группе | ag_part5 | ag_part6 | Где используется |
|-------------------------|----------|----------|-----------------|
| TextAd only (ТГО) | `ct001` | `ag011` | tp1 (РСЯ, `with_shopping=False`), tp2, tp4; UAC МК (tp6) |
| TextAd + ListingAd + ShoppingAd («Т+Л+ТОВ», комбинированный, ЕПК) | `ct010` | `ag011` | tp1 (`with_shopping=True`), **tp5** |
| ShoppingAd + ListingAd only (фид без TextAd, ЕПК) | `ct009` | `ag001` | tp3 |
| UAC Товарные кампании / ТК | `ct010` | `ag001` | **tp7** (UAC; `ct010`+`ag001` отличает от ЕПК `ct010`+`ag011`; значение «ТК» в UAC-контексте — `create_set_plan.py:74-84`) |

Справочник `local_gsheet_naming` (ag_part5): `ct001` = ТГО (только TextAd); в UAC-контексте `ct001`+`ag011` = МК (tp6). `ct009` = «Товарное (Фид/каталог) + Страница каталога» — ShoppingAd+ListingAd **без** TextAd (только tp3); `ct010` = «Комбинированный: ТГО + каталог/фид + страницы каталога» — **TextAd + ShoppingAd + ListingAd** (ЕПК tp1/tp5); в UAC-контексте `ct010`+`ag001` = ТК (Товарные кампании tp7).

**Правило «кодер = реальный состав»:** имя группы ОБЯЗАНО отражать типы объявлений, реально
загружаемых в группу. Реализовано в `_tp1_group_name(ct, r_code, brand, with_shopping, autotarget, tp_code)`:
`with_shopping=False` → `ct001_ag011`; `with_shopping=True` (ЛЮБОЙ `tp_code`, включая `tp5`) →
`ct010_ag011` (ИСПРАВЛЕНО 2026-07-09: промежуточная ветка `ct009_ag001`/без-TextAd для tp5,
записанная как решение «Семён 2026-07-07», была ошибочной и откачена в коде —
`create_set_tp1_builders.py:46-51`); `autotarget=False` → `aoff`, `autotarget=True` → `aon`
(для tp5 группа всегда `aon`, независимо от autotarget-флага). Для `tp2/tp4` по пользовательскому
правилу группа всегда называется через `aon`.

> **Канон для tp1-групп с фидом (`with_shopping=True`):** состав группы = TextAd + ListingAd +
> ShoppingAd («Т+Л+ТОВ») → код **`ct010_ag011`**. Совпадает с эталоном Щербаковой (porg-mjyh6hjv,
> 77 tp1-групп). Прежняя запись `ct009_ag001` для этих групп была ошибочной: ct009 по справочнику
> означает фид БЕЗ TextAd — не соответствовало реальному составу.
>
> **Область действия канона (Семён, 2026-07-17): только слепки ДИРЕКТОЛОГОВ.** Клиентские слепки
> под него НЕ подпадают — у них состав группы диктует живой кабинет-эталон, и `ct009_ag001`
> (чистая товарка ShoppingAd+ListingAd без ТГО) для них правомерен. Пример: `sk_krs`
> («Слепок_sk-krs(бу)», site_type «С пробегом»), эталон — аккаунт `porg-usmc4253`: tp1-кампания
> `712851229`, единственная группа `5773945086` → ShoppingAd=1, ListingAd=1, **TextAd=0**
> (снято зондом v5+Grid, совпало 1:1, 2026-07-17). Перед тем как флагать `ct009_ag001` в tp1 как
> нарушение канона — сначала проверь, чей это слепок.

### 📝 Шаблон Title объявления (A3, 2026-06-22)
Title по умолчанию (дефолт/фолбэк при отсутствии ИИ-title): `«Новые {brand} в {город}. {акция}»`
- Лимит: 35 символов (ЕПК TextAd). Реализован в `_title_from_template(brand, city)`.
- `city` — из `ctx.get("city")` аккаунта, прокидывается через `_create_tp1_campaign`/`_build_text_from_pack`.
- Акция берётся round-robin из `_TITLE_PROMO_POOL` (`_title_from_template` → `_next_title_promo()`).
- Если ИИ дал `ai_title2` (title1 от ИИ) — шаблон НЕ применяется, используется brand[:35].
- Фолбэк: если «Новые {brand} в {город}. {акция}» > 35 симв. → «Новые {brand} в {город}»;
  если и это > 35 — brand[:35].

### tp6 / tp7 (UAC) — кодер ЦЕЛИКОМ на КАМПАНИИ (групп нет)

**Имя кампании** (отображаемое в Директе) — **отдельно от кодера-идентификатора**:
- Формат: `‹базовое имя› - ‹метка таргетинга›` — тип оплаты CPC/CPA/CRM **НЕ пишем**
- Метка таргетинга — одна из 4, выводится из ФАКТА (ключи/аудитории в payload):
  - ключи + аудитории → `КС+аудитории`
  - только аудитории → `аудитории`
  - только ключи → `КС`
  - ничего → `автотаргетинг`
- Примеры: «МК - Кредит - автотаргетинг», «МК - Кредит - КС», «Товарная - Baic - автотаргетинг», «ТК - КС»
- ⚠️ UI-бейдж на странице «Структура слепков» показывает вид таргетинга (как у tp1/tp2), разбирая метку из имени
- ⚠️ Strategy-варианты (cpc+cpa+crm) с одинаковым таргетингом — **дедуп в одну позицию** (не размножать)
- ✅ **Метки применены из staging (2026-07-13):** 56 узлов tp6/tp7 в `slepki_structure.json` получили
  метку в поле `t` из ЖИВОГО факта (staging `keyword_count`/`audience_ids`), не по догадке:
  **RESOLVED** (salamahin/zubakin/tumashenko/karavaev) — достоверно из payload; **HYBRID**
  (terehov/chepelev/pavlov) — по `targeting_mode`. Ручная правка метки по имени/предположению запрещена.

**Позиция-кодер** (поле `gc` в структуре слепка (`direct/slepki/<key>.json`); общий для всех вариантов оплаты):
```
tp6:  {сегмент_ct}_aon_n000_r0000_ct001_ag011_g00
tp7:  {сегмент_ct}_aon_n000_r0000_ct010_ag001_g00
```
`ct001`=МК, `ct010`=ТК (`create_set_plan.py:74-84`). `aon` — всегда (`aoff` в tp6/tp7 не применяется).

### tp1 – tp5 (ЕПК) — кампания ДРОБИТСЯ по (категория × таргетинг)
Одна пара (категория × таргетинг) = **отдельная кампания** (отсюда сотни кампаний на tp — «278 шт»).

**Имя КАМПАНИИ** = `{кодер_кампании} — {тип tp} - {категория} - {таргетинг}`
- кодер кампании = `tp{N}_{cpc|cpa}_{site|kviz}`;
- длинное тире `—` после кодера, дальше дефисы; **область НЕ пишем**.

**Имя ГРУППЫ = ПОЛНЫЙ кодер группы + ` — {Бренд}`** (решение директолога 2026-06-22):
```
ct0181_aoff_n000_r0100_ct001_ag011_g00 — Lada       (TextAd only, tp1 КС/без автотаргета)
ct0019_aoff_n000_r0100_ct001_ag011_g00 — BAIC       (TextAd only, tp1 КС/без автотаргета)
ct0181_aoff_n000_r0100_ct010_ag011_g00 — Lada       (TextAd+ListingAd+ShoppingAd, tp1 КС с фидом)
ct0019_aoff_n000_r0100_ct010_ag011_g00 — BAIC       (TextAd+ListingAd+ShoppingAd, tp1 КС с фидом)
ct0181_aon_n000_r0100_ct001_ag011_g00 — Lada        (TextAd only, tp1 Автотаргетинг / tp2 / tp4)
ct0000_aon_n000_r0100_ct009_ag001_g00 — Товарная галерея  (ShoppingAd+ListingAd only, только tp3/tp7)
```
- ~~суффикс `_NN`~~ **убран по решению директолога (A2, 2026-06-22)** — осознанное расхождение
  с эталоном Щербаковой. В эталоне `_11`/`_21`/`_22`, у нас без суффикса. Код не парсит имя
  обратно — проверено: нет ни одного `split`/`parse` по group name во всём проекте.
- ⚠️ **НЕ «полное отсутствие ключей»** как имя — это была ошибка. Имя группы ОБЯЗАНО нести кодер.
- **Бренд/модель (`ag_part1`, 4-знач `ct`) берётся из КОНТЕНТ-ПАКА M3** — там ct-папки по брендам
  (`feeds_ct_model: ct→марка`), а **НЕ из структуры слепка** (там только `ct0000`). Группы =
  ct-папки пака для (сегмент, tp); у каждой свои ключи/картинки/тексты под бренд.
- `ct0000` (общая, без бренда) — только если в паке для этого tp нет брендовых ct-папок.

> **🧠 ИСТОЧНИК КОНТЕНТА (приоритет — канон):**
> 1. **ИИ на M3 пишет контент** (ключи/минус/тексты/уточнения/картинки) — это ОСНОВНОЙ источник
>    (контент-пак `kontent_pack.py` = вывод ИИ M3, читаем живьём с монта `/opt/neuro_kontent`).
> 2. **Фолбэк → слепки** (`direct_slepok_*` / `direct_ad_templates` в БД Victory) — берём ТОЛЬКО
>    если **M3 недоступен или вернул ошибку/пусто**. Не наоборот.
> Кнопка create_set обязана уметь оба пути: сперва M3, при сбое — слепок, и логировать какой использован.

### Примеры (Щербакова/Мультибренд, autos-kemerovo, r0100)
**Имя группы — ВСЕГДА полный кодер + ` — {Бренд}`** (суффикс `_NN` убран, а НЕ «полное отсутствие ключей»).
Бренд из ct-папок пака M3; если для tp в паке брендов нет — общая `ct0000` + ` — Общая`.
```
tp1_cpc_site — РСЯ - Модели - ключевики (TextAd only, with_shopping=False → ct001, ag011)
   ├ группа: ct0181_aoff_n000_r0100_ct001_ag011_g00 — Lada
   └ группа: ct0019_aoff_n000_r0100_ct001_ag011_g00 — BAIC
tp1_cpc_site — РСЯ - Модели - ключевики (with_shopping=True, TextAd+ListingAd+ShoppingAd → ct010, ag011)
   ├ группа: ct0181_aoff_n000_r0100_ct010_ag011_g00 — Lada
   └ группа: ct0019_aoff_n000_r0100_ct010_ag011_g00 — BAIC
tp2_cpc_site — Поиск - Модели - Автотаргетинг  (TextAd → ct001, ag011)
   ├ группа: ct0181_aon_n000_r0100_ct001_ag011_g00 — Lada
   └ группа: ct0031_aon_n000_r0100_ct001_ag011_g00 — Changan CS35Plus
tp5_cpc_site — Поиск + Товарная галерея - Товарная (TextAd+ListingAd+ShoppingAd, комбинированный → ct010, ag011)
   ├ группа: ct0181_aon_n000_r0100_ct010_ag011_g00 — Lada
   └ группа: ct0019_aon_n000_r0100_ct010_ag011_g00 — BAIC
```
Если в паке для tp нет брендовых ct-папок (только `ct0000`):
```
tp2_cpc_site — Поиск - Модели - Автотаргетинг
   └ группа: ct0000_aon_n000_r0100_ct010_ag011_g00 — Общая
```

> Кодеры берём из структуры слепка (`direct/slepki/<key>.json`; `c`=кодер кампании, `gc`=кодер группы, `gk`=slug группы,
> `t`=тег таргетинга), подставляя реальный `r`-код вместо `r0000`. Каждая кампания разворачивается
> ещё в **cpa**-вариант (`tp1_cpa_site — …`).

### 🗂 gk — ключ группы в per-group модели (2026-07-14)

Поле `gk` в item структуры слепка (`direct/slepki/<key>.json`) — **slug имени группы** (кириллица разрешена).
Отличается от `gc` (кодер позиции): `gc` агрегирует по ct-коду и может совпадать у разных групп
(коллизия gc = НОРМА), `gk` уникален в пределах tp-узла и однозначно адресует конкретный интент.
Item структуры: `{c, t, gc, gk}`.

Пак-путь per-group:
- позитив: `kontent_oktyabr/<seg>/<tp>/<ct>/keywords/<slepok>__<gk>.txt`
- инлайн-минус: `<slepok>__<gk>_minus.txt` (минусы данной группы)
- библиотека слепка: `<slepok>_minus_shared.txt` — **отдельно**, не смешивать с инлайн-минусами групп.

Движок: `read_keywords(..., group=gk)`. `gather()._groups` → `dict[gk → список ключей]`.

Пример масштаба: `kuderko / С пробегом / tp4` = **184 группы** (`Рассрочка|…`, `Автокредит_БУ|…`)
вместо 5 ct-агрегатов — столько `gk`-файлов в ct-папке пака.

### 📦 Фиды (товарные кампании) — ОБЯЗАТЕЛЬНО

Касается кампаний, использующих товарный фид: **tp3** (Товарная галерея), **tp5** (…+Товарная),
**tp7** (Товарка), группа **«Фиды»** в tp1.
(tp4 = Поиск + Динамика — «Динамика» это динамические места показа НА ПОИСКЕ, БЕЗ товарного фида
и без отдельного типа объявлений. По сути поисковая кампания, показ в динамических позициях выдачи.)

1. **Имя фида ОБЯЗАТЕЛЬНО в названии кампании** — добавляется последним сегментом:
   `{кодер} — {тип tp} - {категория} - {таргетинг} - {название_фида}`.
2. **Кампания на КАЖДЫЙ разрешённый фид.** Фидовая кампания **мультиплицируется по глобальному
   allow-list фидов**, а не по случайному первому/любому фиду аккаунта. Allow-list хранится в
   `public.direct_global_feed_rules` и редактируется во вкладке **«Глобальные правила → Фиды»**.

Пример (фиды `target-catalog`, `credit-page`):
```
ТК - автотаргетинг - target-catalog-by-yandex
ТК - автотаргетинг - credit-page-01-a
tp5_cpc_site — Поиск + Динамика + Товарная галерея - Марки - ключевики - target-catalog-by-yandex
```
(tp7 — имя без cpc/cpa, с меткой таргетинга в конце, затем название фида; tp5 — прежний формат с кодером)
> ⚠️ Объём: фидовые tp × N разрешённых фидов аккаунта = много кампаний — учитывать в
> батчинге/лимитах при сборке. Фиды вне глобального allow-list не используются как fallback.

### ⚠️ Текущие черновики назвали НЕВЕРНО
В тестовой сборке Щербаковой (porg-psm5h7q6) tp1–tp5 получили полный код НА КАМПАНИИ
(`tp1_cpc_site_ct0000_…_g00 — …`), а группы — семантические «Марка/Марки/Модели». Это ОШИБКА:
для tp1–tp5 имя кампании должно быть `tp{N}_{pay}_{surf}`, а полный `ct…g00` — на ГРУППАХ.
Исправить при добивке черновиков / в интеграции `create_set`.

## ⚙️ СТРАТЕГИИ / ОПЛАТА / ФИДЫ (канон сборки — обязательно)

### Стратегии — только 2 конверсионные (из «Глобальных правил»)
**Ручные стратегии ЗАПРЕЩЕНЫ** (`HIGHEST_POSITION`, `AVERAGE_CPC` и т.п.). Только:
| Ось имени | Стратегия | Сторона показа |
|-----------|-----------|----------------|
| `cpc` | **AVERAGE_CPA** (оплата за клик, целевой CPA) | tp1 РСЯ → Network (Search=SERVING_OFF); tp2/tp4/tp5 → Search (Network=SERVING_OFF) |
| `cpa` | **PAY_FOR_CONVERSION** (оплата за конверсию) | то же |

- Деньги — в **МИКРО** (₽ × 1e6). Поля СТРОГО по схеме:
  - `AverageCpa: {AverageCpa, GoalId, WeeklySpendLimit, BudgetType: "WEEKLY_BUDGET"}`
  - `PayForConversion: {Cpa, GoalId, WeeklySpendLimit, BudgetType: "WEEKLY_BUDGET"}`
  - `GoalId` = цель формы (напр. 571275452); CPA/бюджет — из `_rule_sets` (cpc_cpa/cpc_budget для cpc, cpa/budget для cpa).
- ⚠️ Текущий `_create_search_test_campaign` (tp2) льёт `HIGHEST_POSITION` — **НАРУШЕНИЕ, переделать** на AVERAGE_CPA/PAY_FOR_CONVERSION.

### Каждый текстовый tp = ПАРА кампаний (cpc + cpa)
Не одна «тестовая». На tp создаются **две** кампании — `tp{N}_cpc_…` и `tp{N}_cpa_…` (как Мастер/Товарка). Набор это уже показывает.

### tp5 — ПОИСК + Товарная галерея (TextAd + ShoppingAd + ListingAd, комбинированный)

> **⚠️ ИСТОРИЯ (важно не перепутать порядок):** эталон с 2026-06-22 — комбинированный (`ct010_ag011`).
> 2026-07-07 было принято промежуточное решение «только ShoppingAd+ListingAd, `ct009_ag001`, БЕЗ TextAd» —
> оно оказалось **ошибочным** и было **откачено в коде** (`create_set_tp1_builders.py:46-51`,
> `:809`: «условие tp5 убрано (2026-07-07)»). Семён подтвердил 2026-07-09: **комбинированный — финал.**
> Кодер группы: `ct010_ag011` (TextAd+ShoppingAd+ListingAd), как у tp1 `with_shopping=True`.

UNIFIED/TEXT_CAMPAIGN, одна cpc-кампания + одна cpa, по каждому бренду — отдельная группа
с **TextAd + ShoppingAd + ListingAd + ключевыми словами** (84–149 ключей на группу, ~20 групп на кампанию).

- **Тип кампании: TEXT_CAMPAIGN** (`_create_search_test_campaign`, `pay="tcpa"|"cpa"`). Не UNIFIED_CAMPAIGN.
- **Канал — только Поиск.** `Network=SERVING_OFF`. РСЯ, Яндекс.Карты — ВЫКЛ.
- **Места показа** (Grid-докрутка): ✅ «Товарная галерея на поиске» + ✅ «Продвижение в поисковой выдаче».
  «Динамические места на поиске» и «Список организаций» — ВЫКЛ.
- **Стратегия** — конверсионная (НЕ ручная): `cpc → pay="tcpa"` (Search=AVERAGE_CPA), `cpa → pay="cpa"` (Search=PAY_FOR_CONVERSION). Поля `AverageCpa/Cpa, GoalId, WeeklySpendLimit, BudgetType:"WEEKLY_BUDGET"`.
- **Группы:** по одной на каждый бренд из контент-пака M3, кодер `ct{N}_aon_n000_{r}_ct010_ag011_g00 — {Бренд}`,
  `aon` ВСЕГДА (независимо от autotarget-флага бренд-группы).
  Каждая группа содержит ключи + релевантное соответствие + **TextAd + ShoppingAd + ListingAd**.
  Реализовано через `_build_tp1_from_pack(with_shopping=True, tp_code="tp5")` — Phase 3 (TextAd) и
  Phase 4 (ShoppingAd+ListingAd) выполняются ОБЕ (условие пропуска Phase 3 для tp5 убрано 2026-07-07).
- **Движок:** `_create_tp5_single` → TEXT_CAMPAIGN → `_build_tp1_from_pack(with_shopping=True, tp_code="tp5")` →
  Grid-финализация + корректировки. Фид-объявления получают `feed_models` с фильтром по коллекции (per-бренд).
- **Расширения:** уточнения через наследуемые campaign-level callouts (Grid), быстрые ссылки — на объявлении.
  На ShoppingAd уточнения напрямую не принимает (API отвергает) — TextAd в той же группе получает их обычным путём.
- **Аудит:** сверить, действуют ли для tp5 те же правила, что для tp1 (`_audit_tp1_adaptive`:
  BUTTON_MISSING/SHORT_TITLES/IMAGE_MISSING/VIDEO_MISSING, 7 заголовков) — не проверено, TODO.

### Фиды — кампания на КАЖДЫЙ разрешённый фид
Фидовые tp (**tp3, tp5, tp7**) мультиплицируются по **разрешённым глобальными правилами фидам**,
которые реально есть в аккаунте. Имя кампании несёт название фида (см. раздел «Фиды»).
Глобальный список хранится в `public.direct_global_feed_rules`; UI редактирования — вкладка
**«Глобальные правила → Фиды»**.

#### Зависимость кол-ва кампаний от числа фидов и галочки «Загружать по одному фиду (/yandex.xml)»
От числа разрешённых фидов **N** (= фиды аккаунта, входящие в allow-list `direct_global_feed_rules`)
зависит кол-во создаваемых кампаний ТОЛЬКО у фидовых tp:

| tp | тип | как зависит от N |
|----|-----|------------------|
| **tp3** ТГ РСЯ (`rsya_gallery`) | fan-out ×N | `_create_tp3_campaign` — пара cpc+cpa на КАЖДЫЙ фид (`create_set_feed_builders.py:1054`) |
| **tp5** Поиск+Динамика+ТГ (`search_gallery`) | fan-out ×N | fan-out в движке `_create_tp5_campaign` (`create_set_feed_builders.py:915`) |
| **tp7** Товарка (UAC) | fan-out ×N | fan-out в плане по всем фидам (`create_set_plan.py:834-840`) |
| **tp1** РСЯ (`tp1_rsy`) | **условно** | обычно 1 РК; фид используется (один, /yandex.xml), но по числу фидов НЕ размножается — на итог не влияет, кроме случаев, когда позиция задействует фид |
| tp2 / tp4 / tp6 | не зависят | по сегментам/группам×оплата, без фидов |

**Галочка «Загружать кампании по одному фиду (/yandex.xml)»** (`single_feed`, по умолчанию ВЫКЛ):
- **ВЫКЛ** → фидовые tp3/tp5/tp7 создаются по **КАЖДОМУ** разрешённому фиду → **×N**.
- **ВКЛ** → только по фиду **`/yandex.xml`** (фид «<сайт> — yandex»); если его нет — по решению
  пользователя первый доступный фид → **×1**. Резолв: `create_set_plan.py:396-440`.

**Оценка итога (упрощённо):**
`Итого = (tp1+tp2+tp4 по сегм/группам) + tp6(группы×оплата) + (tp3+tp5+tp7)×(single_feed? 1 : N)`,
где общий множитель «Под стиль сайта» снят → без CPA-кампаний (только cpc).

**N подтягивает кнопка «⟳ Обновить фиды»** в карточке «Итог создания» (эндпоинт
`/direct/api/account_assets` → `allowed_feeds_count`). До нажатия tp3/tp5/tp7 в оценке = pending «× фиды».
⚠️ Счётчик считает N по URL/имени файла фида — если фид в кабинете назван ЯРЛЫКОМ (не URL), матч
по v5-`Name` даёт ложный 0; корректный источник URL — Grid (`_grid_feeds`), как в `create_set_plan`.

### Картинки по `ct`
- `ct0000, ct0001, ct0002, ct0003, ct0004, ct0005, ct0006, ct0007, ct0008, ct0009, ct0010,
  ct0013, ct0014` — общие/аудиторные группы, всегда берут общий пул картинок `ct0000`.
- `ct0015, ct0016, ct0017, ct0018` — кузова, берут картинки из своей `ct`-папки.
- Бренды/модели берут картинки из своей брендовой/модельной `ct`-папки. Общая РК не должна
  получать модельные картинки.

### AC_TP_BUILD ↔ backend: ключи вариантов ДОЛЖНЫ совпадать
Ключи в `index.html::AC_TP_BUILD` обязаны точно совпадать с backend-вариантами (напр. `tp1_rsy`, а не `rsya`). Рассинхрон → кнопка шлёт несуществующий вариант, создание падает.

### 🧱 Уточнения / быстрые ссылки / минус — КАК на самом деле (проверено по API 2026-06-21)
> Эмпирически: валидные campaign-level поля ЕПК (UNIFIED_CAMPAIGN) =
> `CounterIds, Settings, BiddingStrategy, PriorityGoals, TrackingParams, AttributionModel,
> PackageBiddingStrategy, …, NegativeKeywordSharedSetIds, AdvertisedItem, …`.
- **Уточнений и быстрых ссылок на уровне кампании в API НЕТ** (поэтому правка «поля кампании» падала 2×).
  В Директе они крепятся ТОЛЬКО к объявлению: `TextAd.SitelinkSetId` + `TextAd.AdExtensions=[{AdExtensionId}]`.
- **«На уровне кампании» = один и тот же набор на КАЖДОМ объявлении:** создать 1 SitelinksSet
  (`add_sitelinks_set`) + уточнения-ассеты (`ensure_callouts`, переиспользуют существующие по тексту) →
  привязать их id к каждому `add_text_ad(sitelink_set_id=…, callout_ids=…)`. Визуально/по факту = кампанийные.
- **Минус-слова shared-set (≈257 «Минуса общие 016») — ЭТО реальное campaign-level поле:**
  `NegativeKeywordSharedSetIds` у ЕПК. Привязываем общий набор к кампании, НЕ кладём минус на группы.
  - **Лимиты минус-фраз Директа (символы БЕЗ пробелов, ≤7 слов на фразу). Нет лимита «по числу фраз» — только символы:**
    - **группа** объявлений — **4096** (`AdGroups[].NegativeKeywords`);
    - **минусы НАПРЯМУЮ на кампании** — **20 000** (`Campaigns[].NegativeKeywords`);
    - **библиотечный общий набор** (`negativekeywordsharedsets`, привязка `NegativeKeywordSharedSetIds`) — **4096** (как группа!), до 30 наборов на аккаунт. `NegativeKeywords` в add — ПЛОСКИЙ массив строк, без `{Items}` (v5 ref).
    Источники: `docs/keywords/negative-keywords.md` (платформа) + v5 ref negativekeywordsharedsets/add.
  - **Как делают слепки (микс):** scherbakova — общий библиотечный набор «Минуса общие 016» (~257 слов, ≤4096); pavlov — минусы НАПРЯМУЮ на кампании (~975 слов, ≤20 000). Полный список >4096 в один набор НЕ влезет → для полного нужен campaign-direct, для общего — shared-set.
  - ⚠️ **tp2/tp4 исторически делали НЕ так:** клали минус на группы с заглушкой `_kw_clean(minus, 100)`
    (терялся хвост: pavlov 975 → входило 100). Канон — campaign shared-set, как у tp1/tp5.
  - ✅ **Cookie-путь tp2/tp4 — слепковый `_minus_shared` INLINE (2026-07-13, ERRORS_JOURNAL #9):**
    `_create_text_via_cookie` (tp2/tp4 via_cookie) теперь кладёт минус-слова слепка (`_minus_shared`)
    INLINE в `spec.minusKeywords` (раньше на cookie-пути шли только глобальные слова → слепковый
    минус терялся). Общий Grid-набор (shared-set) при этом НЕ мутируется — минус идёт inline на кампанию.

### 🗂 Только ЧЕРНОВИКИ (персонализация-конфликт снят)
**Кнопка/движок ВСЕГДА создают черновики (`launch=False`).** Конфликт «персонализация вкл/выкл у реальных
слепков vs наш инвариант ВЫКЛ» **снят**: мы не публикуем — директолог открывает черновик и финализирует сам.
Поэтому держим наш канон (инварианты блек-листа), боевой запуск из движка НЕ делаем.

### 🛡 Защита от дублей (повторный клик не плодит кампании)
Перед созданием — `filter_new_campaign_names(имена)` сверяет с ВСЕМИ кампаниями аккаунта (любой State,
вкл. черновики/архив) по точному имени → существующие ПРОПУСКАЕМ, создаём только новые. Лог: что создано / что пропущено.

## 🔗 ГИБРИД tp1–tp5: v5 каркас + Grid-докрутка куками (проверено live 2026-06-21)

Официальный **v5** строит КАРКАС (кампания + стратегия AVERAGE_CPA/PAY_FOR_CONVERSION + группы + фид-объявления + быстрые ссылки на объявлении). Но **места показа и ассеты кампании v5 НЕ умеет** — их добавляем **куками через Grid** (`direct.yandex.ru/web-api/grid/api`), как UAC для tp6/tp7.

**Вся докрутка = ОДНА мутация `UpdateCampaigns`** (полный объект кампании, partial API отвергает — `attributionModel NonNull`; делаем read-modify-write либо базовый шаблон + override). Поля:
- **Места показа** `biddingStategyWithPlatforms.platforms`: `{gallery:true (товарная галерея), search:true (продвижение в выдаче), network:false (РСЯ off), yandexMaps:false (Карты off), serpGeoWizard:false, organic:true}`. ⚠️ Карты выключаются `yandexMaps:false`, а НЕ v5 `ENABLE_COMPANY_INFO`.
- **Уточнения на кампании** (наследуемые): `inheritableCallouts:{calloutIds:[...]}`. ← «уточнения на уровне кампании», которых нет в v5; основной путь для фид-объявлений.
- **Быстрые ссылки на кампании**: `inheritableSitelinkSet:{sitelinkSetId:<id>}`.
- **Промо**: `promoExtensionId:<id>`. **Корректировки**: `bidModifiers:{...}`.
- **Инварианты**: `isAlternativeTextsEnabled:false` (персонализация), `hasSiteMonitoring:true` (мониторинг), `hasExtendedGeoTargeting:false` (расш.гео), `isRecommendationsManagementEnabled:false` («Директ помогает» выкл).
- Мутация: `mutation UpdateCampaigns($input:GdUpdateCampaignsInput!$login:String!){...}`; `strategyId:null` → пересобирается по `strategyData`. CSRF: первый POST 403 → берём `_direct_csrf_token` → ретрай.

## 🏭 ПРОД (кнопка create_set) — tp5 комбинированная (2026-06-22)
- UI `index.html::AC_TP_BUILD` `5:['search_gallery']` → план → диспетчер `api_create_set`.
- Диспетчер: ветка `it.type=='search_gallery'` → **`_create_tp5_campaign`** (с параметром `agency`).
- `blueprint.py::_create_tp5_campaign` → пара cpc(`pay="tcpa"`)+cpa(`pay="cpa"`) по каждому фиду аккаунта.
  Каждая: **TEXT_CAMPAIGN** (`_create_search_test_campaign`) → `_build_tp1_from_pack(with_shopping=True)` →
  `grid_finalize.GridClient.finalize` → `grid_finalize.apply_corrections`
  (ПОРЯДОК строгий: Grid перезаписывает bidModifiers).
- `grid_finalize.py` — Grid-докрутка (места показа `placementTypes`=«Ручная», inheritableCallouts/SitelinkSet,
  promo, libraryMinusKeywordsIds, инварианты) + `set_default_text` (UpdateShoppingAds.bodies) + `apply_corrections`.
- Шаблоны: `grid_uc_template.json`, `grid_uc_mutation.graphql`, `grid_shopping_mutation.graphql`.
- **tp2 legacy** (`_create_search_test_campaign`) — стратегия `HIGHEST_POSITION` (нарушение канона);
  переделать на `search_cpa`/`search_payconv`.
- **fan-out по фидам:** пока берётся первый URL-фид; итерация по всем фидам аккаунта — следующий шаг.

### Fan-out фиды→модели→коллекции (tp3/tp5/tp7)
При создании товарных кампаний blueprint мультиплицирует по разрешённым фидам аккаунта:
- Фиды из `_do_assets(login, agency)` → список `feeds` → фильтр `_global_feed_rules`.
- На КАЖДЫЙ разрешённый фид — отдельная кампания; имя содержит название фида (см. раздел «Фиды»).
- Коллекции (`collection_id` вида `model_N` из listings фида): на каждую модель — отдельная группа +
  `ShoppingAd`/`ListingAd` с `FeedFilterConditions=[{Operand:"collectionId", …}]` (фильтр на модель).
  Реализовано в `campaign.py::add_shopping_ad`/`add_listing_ad(collection_id=…)`.
- **Текущий статус:** глобальный allow-list фидов реализован; `tp5` добавляет имя фида в кампанию.

## Связанный код
- `campaign.py::ensure_callouts` — гарантировать уточнения (CALLOUT-ассеты), переиспуск по тексту → id.
- `campaign.py::add_text_ad(sitelink_set_id, callout_ids)` — TextAd с быстрыми ссылками + уточнениями.
- `campaign.py::add_shopping_ad(adgroup_id, feed_id, collection_id)` — ShoppingAd с опциональным FeedFilterConditions по collectionId.
- `campaign.py::add_listing_ad(adgroup_id, feed_id, collection_id)` — ListingAd («страницы каталога» фида), аналогичный синтаксис.
- `campaign.py::filter_new_campaign_names` — защита от дублей (сверка по имени, любой State).
- `blueprint.py::_build_name` — сборка имени для tp6/tp7 (новый формат: `{базовое имя} - {метка таргетинга}`; без cpc/cpa в имени; метка = автотаргетинг/КС/аудитории/КС+аудитории).
- `blueprint.py::_resolve_region` — `r0000` → реальный r-код + область словами.
- `kontent_pack.py::videos_for_login(login)` — видео слепок-сборки аккаунта с M3 (по суффиксу логина → `_slepki_data/<бренд>_<город>_<хэш>/videos/`).
- `direct/slepki/<key>.json` (+ `direct/slepki/_order.json`) — структура групп слепков с кодерами (поля `c`=кодер кампании, `gc`=кодер группы, `gk`=slug группы, `t`=тег таргетинга); собирается `slepki_store.assemble()`, монолита `slepki_structure.json` нет.
