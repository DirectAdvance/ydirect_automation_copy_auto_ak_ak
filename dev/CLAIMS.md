# CLAIMS.md — реестр «застолблённых» файлов (анти-коллизии двух окон)

> Канон: `docs/WORKTREE_WORKFLOW.md` → «КАНОН: claims + git-diff-ревью».
> **Перед правкой** допиши строку. **Второе окно обязано свериться** и не лезть в
> застолблённое (взять другие файлы или подождать). Завершил → статус `done` или удали строку.
> Ревью+merge в main делает `domovoy` (это `home/`) по `git diff` — НЕ чтением файла целиком.

## Активные claim'ы

| окно | файлы / регион | с (дата-время, ЕКБ) | задача | статус |
|------|----------------|---------------------|--------|--------|
| — | — | — | — | — |

## Журнал (завершённые)

| окно | файлы | задача | завершено | влито в main |
|------|-------|--------|-----------|--------------|
| B | blueprint.py (минусы tp2/tp4: `_SLEPOK_MINUS_MODE`, `_apply_campaign_direct_minus`, `_build_tp2_adgroups`, `_TEXT_ENGINE`) | минус per-slepok как у реальных слепков | 2026-06-22 | ⚠️ код есть, digest.service НЕ перезапущен |

---

### Бэкап-снапшоты (откат, НЕ механизм merge)
Полные копии правленых файлов с таймстампом — в `dev/snapshots/` (вне git и Mutagen).
Пример: `dev/snapshots/blueprint.py.20260622_1900`. Это только для отката, сливаем через git-diff.
