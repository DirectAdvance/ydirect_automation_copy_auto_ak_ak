# Защищённые правила создания РК (спец-логика «Структура слепков» → «Создание РК»)

> Источник правды для реализации задачи 7. Эти правила — **защищённые**: движок создания
> обязан их соблюдать при генерации кампаний из структуры слепка. В UI «Глобальные правила»
> для них планируется отдельная вкладка «Защищённые правила» (только чтение бизнес-логики).
> Подтверждено Семёном 2026-07-15.

---

## Правило 1 — тег «х3» на tp1 → три кампании

Если у кампании **tp1 (РСЯ)** проставлен тег **«х3»**, то при создании из этой одной позиции
структуры создаются **ТРИ кампании**:

1. **По ключам** — только ключевые слова (КС), автотаргетинг выключен.
2. **По автотаргетингу** — только автотаргетинг, без ключей.
3. **По ключам + автотаргетингу** — и ключи, и автотаргетинг вместе.

- Тег «х3» — управляющий (не отображение): одна позиция структуры → три РК на выходе.
- Именование каждой из трёх — по общему кодеру tp1 + метка режима (КС / автотаргетинг / КС+автотаргетинг).
- Без тега «х3» tp1 создаётся как одна кампания по своему фактическому режиму.

## Правило 2 — тег «все фиды» → все фиды группами

Если у кампании проставлен тег **«все фиды»**, то в эту кампанию **добавляются группами ВСЕ
разрешённые фиды аккаунта** (одна группа на фид), а не один/дефолтный фид.

- Применяется к фидовым типам (tp3 товарная галерея, tp5 П+Д+ТГ, tp7 товарная — где фид является осью).
- Без тега «все фиды» — поведение по умолчанию (явно заданный фид позиции, иначе безопасный fan-out/дефолт по текущей логике create_set_plan).
- «все фиды» — это явное указание размножить по всем фидам группами внутри ОДНОЙ кампании.

---

## Уточнения (согласовано Семёном 2026-07-15)

- **Охват 1:1:** «Создание РК» = «Структура слепков» 1:1 по ВСЕМ обычным tp1–tp7 для выбранного
  слепка+тип сайта. Для пакета **«Посевы»** аналогичный источник правды — `direct/slepki/posevy.json`
  (tp8/tp9/tp10), и create-tab обязан показывать то же дерево, что `/direct/automation/slepki`.
  НО **весь текущий функционал сохраняется — ломать нельзя** (см. ниже).
- **camp_names-папка → кампания:** как уже реализовано — **черновики** (State=OFF), отдельная
  кампания с **двумя настройками стратегии** (cpc+cpa). Функционал не менять.
- **Правило 1 (х3):** КС / автотаргет / КС+автотаргет — 3 РК. **Каждой — ПОЛНЫЙ бюджет** (из
  глобальных правил, как обычная отдельная кампания; не делёж). Только tp1.
- **Правило 2 (все фиды):** применяется к **tp3, tp5, tp1-РСЯ** (Смарт-баннеры/фид). **tp7 — НЕ
  входит:** товарка сохраняет текущее поведение фида (явный фид позиции / fan-out по разрешённым).
- **Фидовые минус-марки/модели:** глобальные feed-minus из «Глобальных правил» НЕ применяются к
  `site_type='С пробегом'`. Для БУ-фидов это валидный ассортимент, а не мусор. В tp7 глобальные
  feed-minus не применяются вообще: марка/модель ограничиваются только positive-фильтром из ct,
  `ct0000` остаётся без feed-filter.
- **tp1 products-only fallback:** если для выбранного `site_type` нет структуры/camp_names и движок
  берёт fallback из другого auto site_type, процедурные `Фиды`/`Смарт-Баннер` обязаны сохранять
  сегмент `Марки/Модели/Общее`. `tp1_segment=None` для таких campaigns запрещён, потому что смешивает
  общие, марочные и модельные группы.
- **Теги, управляющие созданием — ТОЛЬКО «х3» и «все фиды».** Все остальные теги
  (КС/автотаргетинг/аудитории/фид/кастомные из реестра) — **отображение/классификация**, на
  создание НЕ влияют.
- **Регион:** структура шаблонная `r0000`; реальный город подставляется на создании (как сейчас).

### Сохраняемый функционал (не ломать)
Черновики State=OFF · две стратегии cpc+cpa (по галке «под стиль сайта») · галочки выбора позиций ·
резолв региона по городу аккаунта · контент из пака (site,tp,ct,slepok,gk) · аудитории/интересы из
позиции · докрутка 152/units · brand-first нейминг + символьные бюджеты · feed_alert (нет фидов).

## Минус-слова при создании (согласовано Семёном 2026-07-15)

- **Наборы минус слов:** используем **ОДИН общий** shared-набор (как tp1/tp5 сейчас — «Минуса общие»).
  Если в структуре у кампании указано **2+ наборов — слить их в ОДИН** shared-набор.
  Минусы ИЗ набора применяются **только на tp2–tp5** (поисковые).
- **Глобальные минусы** (из Глобальных правил, напр. «отзывы»): **ко ВСЕМ обычным кампаниям
  (tp2–tp7; tp1 исключён ниже)**,
  на уровне кампании. **Исключение (правило Семёна, 2026-07-22, F3):** tp1 (РСЯ) campaign-level
  минус-слова НЕ ставятся — `_apply_campaign_direct_minus` вызов убран из
  `create_set_tp1_builders.py`; tp2/tp4/tp5 не тронуты.
- Для `tp8/tp9/tp10` это не search/feed-minus контур: Посевы идут через `GdPostCampaign`, а
  демографические корректировки применяются отдельно с лимитом Grid `-50%`.
- **Объединение:** **наборы + глобальные — вместе** (на кампанию); **pack-минусы — отдельно**
  (на группы, как сейчас `_collect_pack_minus`). Наборы(tp2-5)+глобальные(все) не смешиваются с pack.

## Дельта реализации (из diff-аудита 2026-07-15)

- **Контракт = `create_set_plan.py:_set_plan_response`** (не JS-дерево) — он строит `plan[]`→`items[]`
  для исполнителя. Это единственная точка, где определяются создаваемые кампании.
- **tp6/tp7** — уже 1:1 из структуры (per-item `_emit_struct`) — **эталон для остальных**.
- **tp1–tp5** — сейчас **сегмент-коллапс** (Марки/Модели/Общее × режимы), НЕ `camp_names` → расходится
  с деревом. tp3 схлопнут в 1 РК «ТГ - Фид (товары)».
- **План:** вынести общий `structure_to_campaigns(slepok, site_type, tp)` (зеркало
  `routes_slepki_edit.py:_build_export_rows`, campaign = `item.camp_names`), звать из превью И из
  `_set_plan_response`; поверх — теги «х3» (tp1→3, читать из `campaign_tags`, не из `seg_modes`) и
  «все фиды» (tp3/tp5/tp1-РСЯ → все разрешённые фиды группами; НОВОЕ, нигде нет). Исполнитель
  (`create_set_orchestrator`) — НЕ трогать (он per-item, агностичен к группировке).
- **⚠️ Сохранить (load-bearing):** per-group контент по ct/gk (иначе «0/184 групп») · profile-гейтинг
  `_slepok_profile_excludes_tp` / `_slepok_tp_modes=[]` · процедурные добавки (tp4 donor «Модели»,
  tp1 «Смарт-Баннер/Фиды», tp5 «Фиды») · `pays` cpc+cpa · dedup `_uniq`/`emitted_tp67` · feed_alert ·
  source_manifest (gen_ses) · поля tp6/7 для `_run_master_product_item` · draft-only State=OFF.
- **Live content fallback:** stream-generation не имеет права копировать `direct_slepok_content` как
  финальный live-контент. При пустом/неполном ответе LLM orchestrator пишет warning и продолжает в
  builder fallback/локальные шаблоны; если builder тоже не собрал валидный контент/пак — item падает
  с конкретным content-gap. Полностью мёртвый content-пайплайн блокируется до Direct-мутаций.

## Статус

- Реализация — задача 7. **diff-аудит выполнен** (см. «Дельта» выше). Следующий шаг — точечные
  правки по дельте (после согласования DoD). Пока **спека**, не код.
- Теги «х3» и «все фиды» уже присутствуют в UI структуры (бейджи). Здесь зафиксирована их
  СЕМАНТИКА для движка создания.
