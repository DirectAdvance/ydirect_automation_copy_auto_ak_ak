# Бот «Слепки Мастер» — принятые решения (2026-07-29)

> Telegram-бот для загрузки/обновления слепков нейродиректолога
> (`seoadvanced.ru/direct/automation/slepki`). Токен: `.secret/.env` → `TG_SLEPKI_MASTER_BOT`.
> Этот файл — контракт. Реализация сверяется с ним, а не с памятью сессии.

## Решения Семёна

| Развилка | Решение |
|---|---|
| Кто разбирает неоднозначное | **Codex headless на LXC 101** (`codex exec`), 24/7, без моей сессии |
| Применение правок | **Всегда proposal + кнопка «Применить»** (review-first, правило проекта) |
| Координаты пака | **Авто-детект + подтверждение** одним сообщением с кнопками правки |

## Разделение труда

```
Telegram (Семён)
   │  логин аккаунта  |  xlsx/архив/картинки  |  имя папки в _inbox/
   ▼
Бот (LXC 101, :пока не занят)  ── только приём, валидация, статус, кнопки
   ▼  job в очередь (механика direct-slepki-worker)
Воркер БЕЗ ИИ ── харвест по API/Grid · парс xlsx · распаковка · раскладка · бэкапы
   ▼  неоднозначное
codex exec на 101 ── маппинг структуры, ct-коды, спорные имена → proposal
   ▼
Бот показывает diff «было → стало» + [Применить] / [Отклонить]
   ▼  только после кнопки
Apply: структура → direct/slepki/<key>.json (+_order.json), контент → M3 kontent_oktyabr
```

Claude Code (я) подключаюсь на нетиповых слепках, разборе сбоев и ревью proposal.

## Два слоя хранения (не путать)

| Слой | Источник правды | Адрес |
|---|---|---|
| Структура кампаний/групп | `direct/slepki/<key>.json` + `_order.json` | по ключу слепка |
| Контент («пак») | **M3** `kontent_oktyabr/<Сегмент>/tpX/<ct-код>/` | `keywords/<slepok>.txt`, `<slepok>_minus.txt`, `callouts/<slepok>.txt`, `image_slepki.txt` |

- Пак **один** (`kontent_oktyabr`). «В какой пак» = 4 координаты: `slepok / site_type(Сегмент) / tp / ct`.
- ct-код — только из `public.local_gsheet_naming`. `coder_autosalons_v16__нейминг.csv` — НЕ источник.
- Писать контент **в M3**: ночной синк (00:00/12:00) M3→LXC101 перезатирает локальное зеркало.
- Апдейт vs новый решается механически: есть `slepki/<key>.json` → апдейт (бэкап `_backup_*` + merge); нет → новый.

## Именование нового слепка

- `key` — латиница-slug: фамилия директолога (`kuderko`, `chepelev`) либо бренд-slug для безличного
  кабинета (`gen_ses`, `avto_sk`, `sk_krs`, `dmp`). Уникальность проверять по `slepki/`.
- `name` — `Слепок_<Фамилия>` / `Слепок_<Бренд>`.
- `ui_group` — `auto` (салоны) | `auto_other` | `other`; `ruleset` — `auto` | `custom` (B2B, см. skill
  `direct-custom-slepok`).
- Бот предлагает `key`/`name` сам (транслит из логина/кабинета), Семён может переименовать одним сообщением.

## Входы

1. **Логин аккаунта** → живой харвест: API v5 + Grid по кукам (tp6/tp7). База: агент
   `direct_content_harvester`, скил `slepki-harvest-fleet`.
2. **Файлы**: XLSX-экспорты Директа + картинки. Прецедент — `scripts/extract_gen_ses_archive.py`
   (слепок `gen_ses` собран из xlsx + скринов через детерминированный манифест).

### ⚠️ Лимит Telegram
Бот скачивает файл **не больше 20 МБ** (Bot API `getFile`). Поэтому:
- мелкие файлы — прямо в чат;
- крупный архив — Семён кладёт в `home/seoadvanced/direct/_inbox/<имя>/` (синкается Mutagen на LXC 101),
  боту шлёт только имя папки. Ссылку на локальный путь Мака бот открыть не может в принципе.

## UI бота — кнопки (не команды-строки)

Всё, что можно нажать, — кнопка. Текстом Семён вводит только то, что нельзя выбрать (логин, имя слепка).

**Нижнее меню (ReplyKeyboard, всегда видно):**
`🆕 Новый слепок` · `♻️ Обновить слепок` · `📦 Статус паков` · `📋 Очередь` · `❓ Помощь`

**Инлайн-кнопки по шагам:**

| Шаг | Кнопки |
|---|---|
| Выбор входа | `🔑 По логину` · `📎 Файлы (xlsx/картинки)` · `📂 Папка в _inbox` |
| Подтверждение координат | `✅ Верно` · `✏️ Слепок` · `✏️ Сегмент` · `✏️ tp` · `✏️ ct` |
| Выбор слепка / сегмента / tp | список существующих значений кнопками (по 2–3 в ряд), стр. навигация `◀ ▶` |
| Новый слепок | `Имя: <предложено>` → `✅ Принять` · `✏️ Переименовать`; затем `ruleset`: `auto` · `custom`; `ui_group`: `auto` · `auto_other` · `other` |
| Proposal | `✅ Применить` · `👀 Показать полный diff` · `❌ Отклонить` |
| Job в работе | `🔄 Обновить статус` · `🛑 Отменить` |
| После apply | `📄 Отчёт` · `↩️ Откатить (бэкап)` |

Правила: `callback_data` ≤64 байт → в неё кладём `job_id` + короткий код действия, состояние —
в БД джобы, не в кнопке. Нажатая кнопка сразу гасится (`edit_message_reply_markup`), чтобы не было
двойного apply. Кнопки видит только allowlist-chat, чужому — молчание.

## Открытые пункты (до реализации)

- [ ] `TG_SLEPKI_MASTER_CHAT` пуст — заполнить после первого сообщения боту (`getUpdates`).
- [ ] Allowlist: бот принимает команды только от chat_id Семёна, остальным — отказ.
- [ ] M3 сейчас недоступна с Мака (Tailscale переключён на личный аккаунт) — писать в пак только
      с LXC 101 через `m3-relay`.
- [ ] Порт/юнит бота на LXC 101 (`direct-slepki-bot.service`) — выбрать свободный.
